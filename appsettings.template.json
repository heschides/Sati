{
  "//": "Template for the real appsettings.json, which is gitignored because it holds the local Production connection string. Copy this file to appsettings.json in the project root before the first run on a new machine. A fresh clone has no appsettings.json, so startup stops before database access because the SatiProduction connection string is not configured.",

  "//split": "Non-secret deployment names and endpoints live in appsettings.Public.json, which IS tracked. Only the workstation-specific Production connection string belongs here.",

  "//ConnectionStrings": "DataEnvironmentResolver reads the connection string named SatiProduction and cross-checks its Initial Catalog against DataEnvironments:Production:ExpectedDatabaseName in appsettings.Public.json. The two must name the same database or startup refuses.",

  "ConnectionStrings": {
    "SatiProduction": "Server=(localdb)\\MSSQLLocalDB;Database=SatiProduction;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=True"
  },

  "//database": "SatiProduction must exist and carry the dbo.SatiDatabaseIdentity marker BEFORE first run: DatabaseIdentityValidator runs ahead of Database.Migrate(), so pointing at an empty database fails closed rather than building one. Create the local environments with scripts/Provision-LocalDataEnvironments.ps1.",

  "//LocalAi": "Local case-note drafting. Enabled=false removes the feature from the note-entry UI without touching XAML or DI. The first drafting request downloads the ModelAlias variant (multi-gigabyte) into %LOCALAPPDATA%\\Sati\\LocalAi.",

  "LocalAi": {
    "Enabled": false,
    "ModelAlias": "phi-4-mini",
    "MaxInputWords": 500,
    "MaxOutputTokens": 900,
    "RulesFile": "AI_CASE_NOTE_RULES.md"
  }
}
