<Application x:Class="Sati.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Sati.Converters"
             xmlns:children="clr-namespace:Sati.ViewModels.Children"
             xmlns:viewmodels="clr-namespace:Sati.ViewModels"
             xmlns:supervisor="clr-namespace:Sati.ViewModels.Supervisor"
             xmlns:supervisorVms="clr-namespace:Sati.ViewModels.Supervisor"
             xmlns:billingVms="clr-namespace:Sati.ViewModels.Billing"
             xmlns:clientDocumentVms="clr-namespace:Sati.ViewModels.ClientDocuments"
             xmlns:adminVms="clr-namespace:Sati.ViewModels.Admin"
             xmlns:views="clr-namespace:Sati.Views"
             xmlns:clientDocumentViews="clr-namespace:Sati.Views.ClientDocuments"
             xmlns:billingViews="clr-namespace:Sati.Views.Billing">
    <Application.Resources>
        <ResourceDictionary>

            <!-- Theme + state colors. States is theme-independent; the second entry
                 is the ACTIVE theme — swap this one dictionary (or its Source) to
                 retheme. DynamicResource refs throughout the app re-resolve on swap. -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/States.xaml" />
                <ResourceDictionary Source="Themes/SunlitShell.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Fonts -->
            <FontFamily x:Key="AppFont">Inter</FontFamily>
            <FontFamily x:Key="NarrativeFont">Cambria</FontFamily>

            <!-- Global styles -->
            <Style x:Key="LabelStyle"
                   TargetType="TextBlock">
                <Setter Property="FontSize"
                        Value="11" />
                <Setter Property="FontWeight"
                        Value="SemiBold" />
                <Setter Property="Foreground"
                        Value="{DynamicResource TextSecondaryBrush}" />
                <Setter Property="Margin"
                        Value="0,0,0,4" />
            </Style>

            <Style x:Key="SectionHeader"
                   TargetType="TextBlock">
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="FontWeight"
                        Value="Bold" />
                <Setter Property="Foreground"
                        Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="Margin"
                        Value="0,0,0,8" />
            </Style>

            <!-- CaretBrush and SelectionBrush are set explicitly: the framework
                 defaults are system colors, so on dark themes the caret and the
                 selection highlight would otherwise be invisible against a dark
                 surface. Local TextBox styles must use BasedOn to keep this. -->
            <Style TargetType="TextBox">
                <Setter Property="Padding"
                        Value="8,4" />
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="Background"
                        Value="{DynamicResource SurfaceBrush}" />
                <Setter Property="BorderBrush"
                        Value="{DynamicResource InputBorderBrush}" />
                <Setter Property="BorderThickness"
                        Value="1" />
                <Setter Property="Foreground"
                        Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="CaretBrush"
                        Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="SelectionBrush"
                        Value="{DynamicResource AccentBrush}" />
                <Setter Property="SelectionTextBrush"
                        Value="{DynamicResource OnAccentBrush}" />
                <Setter Property="Margin"
                        Value="0,0,0,10" />
                <Style.Triggers>
                    <!-- The framework's disabled TextBox chrome paints a fixed
                         light gray. Dim the themed surface instead so disabled
                         text stays readable against it on dark themes. -->
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Background" Value="{DynamicResource SurfaceMutedBrush}" />
                        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                    </Trigger>
                    <Trigger Property="IsReadOnly" Value="True">
                        <Setter Property="Background" Value="{DynamicResource SurfaceMutedBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="PasswordBox">
                <Setter Property="Padding"
                        Value="8,4" />
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="Background"
                        Value="{DynamicResource SurfaceBrush}" />
                <Setter Property="BorderBrush"
                        Value="{DynamicResource InputBorderBrush}" />
                <Setter Property="BorderThickness"
                        Value="1" />
                <Setter Property="Foreground"
                        Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="CaretBrush"
                        Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="SelectionBrush"
                        Value="{DynamicResource AccentBrush}" />
                <Setter Property="Margin"
                        Value="0,0,0,10" />
            </Style>

            <!-- ComboBox is fully retemplated rather than restyled. The framework
                 template builds its closed-state chrome from a ToggleButton whose
                 own style HARD-CODES a near-white background, and drops the list
                 into a popup painted with SystemColors.WindowBrush. Neither honors
                 the ComboBox's Background, so on dark themes the selected item and
                 every list row rendered as light text on near-white. Setting
                 Background alone cannot reach either surface; the template can. -->
            <Style TargetType="ComboBoxItem">
                <Setter Property="Padding" Value="9,6" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="ItemBorder"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource HoverStrongBrush}" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource AccentBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource OnAccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="Padding"
                        Value="9,4" />
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="MinHeight"
                        Value="36" />
                <Setter Property="Background"
                        Value="{DynamicResource SurfaceRaisedBrush}" />
                <Setter Property="BorderBrush"
                        Value="{DynamicResource InputBorderBrush}" />
                <Setter Property="BorderThickness"
                        Value="1" />
                <Setter Property="Foreground"
                        Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="Margin"
                        Value="0,0,0,10" />
                <Setter Property="HorizontalContentAlignment"
                        Value="Stretch" />
                <Setter Property="VerticalContentAlignment"
                        Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <Border x:Name="ComboBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4"
                                        SnapsToDevicePixels="True">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!-- These four must be full bindings, not TemplateBinding.
                                             ComboBox writes SelectionBoxItem and
                                             SelectionBoxItemTemplate through read-only property keys
                                             AFTER the template is applied, and a TemplateBinding does
                                             not follow that later write. With TemplateBinding a
                                             DisplayMemberPath selection renders as the item's
                                             ToString() — a record shows as
                                             "ThemeOption { DisplayName = ... }". An explicit
                                             ItemTemplate happens to survive, which is what made the
                                             defect look intermittent. -->
                                        <ContentPresenter x:Name="ContentSite"
                                                          Grid.Column="0"
                                                          Margin="{TemplateBinding Padding}"
                                                          Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource TemplatedParent}}"
                                                          ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource TemplatedParent}}"
                                                          ContentTemplateSelector="{Binding ItemTemplateSelector, RelativeSource={RelativeSource TemplatedParent}}"
                                                          ContentStringFormat="{Binding SelectionBoxItemStringFormat, RelativeSource={RelativeSource TemplatedParent}}"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="Center"
                                                          IsHitTestVisible="False" />

                                        <!-- Editable mode is unused today, but the part
                                             must exist or IsEditable silently breaks. -->
                                        <TextBox x:Name="PART_EditableTextBox"
                                                 Grid.Column="0"
                                                 Margin="{TemplateBinding Padding}"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 Foreground="{TemplateBinding Foreground}"
                                                 CaretBrush="{TemplateBinding Foreground}"
                                                 VerticalContentAlignment="Center"
                                                 Visibility="Collapsed" />

                                        <Path x:Name="Arrow"
                                              Grid.Column="1"
                                              Margin="6,0,10,0"
                                              VerticalAlignment="Center"
                                              Data="M 0 0 L 4 4 L 8 0 Z"
                                              Fill="{DynamicResource TextSecondaryBrush}" />
                                    </Grid>
                                </Border>

                                <!-- Transparent full-surface toggle. Keeping it above
                                     the chrome means the whole field opens the list,
                                     matching the framework's click target. -->
                                <ToggleButton x:Name="ToggleSurface"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Focusable="False"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent" />
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>

                                <Popup x:Name="PART_Popup"
                                       Placement="Bottom"
                                       PlacementTarget="{Binding ElementName=ComboBorder}"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       PopupAnimation="Fade"
                                       Focusable="False">
                                    <Border Background="{DynamicResource SurfaceBrush}"
                                            BorderBrush="{DynamicResource InputBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            MinWidth="{TemplateBinding ActualWidth}"
                                            MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                            SnapsToDevicePixels="True">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEditable" Value="True">
                                    <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                                    <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed" />
                                    <!-- Pull the toggle back to the arrow so the
                                         editable field stays typeable. -->
                                    <Setter TargetName="ToggleSurface" Property="HorizontalAlignment" Value="Right" />
                                    <Setter TargetName="ToggleSurface" Property="Width" Value="28" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                    <Setter TargetName="ComboBorder" Property="BorderThickness" Value="2" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="ComboBorder" Property="Background" Value="{DynamicResource SurfaceMutedBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
                                    <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource TextMutedBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Global themed date entry. DatePickerTextBox keeps the parsing,
                 selection, validation, and keyboard behavior of DatePicker; the
                 calendar button is a real, accessible template part. -->
            <Style TargetType="DatePickerTextBox">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="CaretBrush" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="9,5" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DatePickerTextBox">
                            <Grid Background="Transparent">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center" />
                                <TextBlock x:Name="DatePlaceholder"
                                           Text="MM/DD/YYYY"
                                           Margin="{TemplateBinding Padding}"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           FontStyle="Italic"
                                           Opacity="0.48"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="Text" Value="">
                                    <Setter TargetName="DatePlaceholder" Property="Visibility" Value="Visible" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.55" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="DatePicker">
                <Setter Property="FontFamily" Value="{DynamicResource AppFont}" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="MinHeight" Value="36" />
                <Setter Property="Background" Value="{DynamicResource SurfaceRaisedBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="Margin" Value="0,0,0,10" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DatePicker">
                            <Grid x:Name="PART_Root">
                                <Border x:Name="DateBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="36" />
                                        </Grid.ColumnDefinitions>

                                        <DatePickerTextBox x:Name="PART_TextBox"
                                                           Grid.Column="0"
                                                           Background="Transparent"
                                                           Foreground="{TemplateBinding Foreground}"
                                                           FontFamily="{TemplateBinding FontFamily}"
                                                           FontSize="{TemplateBinding FontSize}"
                                                           HorizontalContentAlignment="Stretch"
                                                           VerticalContentAlignment="Center" />

                                        <Button x:Name="PART_Button"
                                                Grid.Column="1"
                                                Width="36"
                                                MinHeight="34"
                                                Padding="0"
                                                Background="Transparent"
                                                BorderBrush="{DynamicResource DividerBrush}"
                                                BorderThickness="1,0,0,0"
                                                Focusable="False"
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Stretch"
                                                AutomationProperties.Name="Open calendar"
                                                ToolTip="Open calendar">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="CalendarButtonSurface"
                                                            Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                            CornerRadius="0,3,3,0">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center" />
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="CalendarButtonSurface"
                                                                    Property="Background"
                                                                    Value="{DynamicResource HoverBrush}" />
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter TargetName="CalendarButtonSurface"
                                                                    Property="Background"
                                                                    Value="{DynamicResource SurfaceAltBrush}" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                            <TextBlock Text="&#xE787;"
                                                       FontFamily="Segoe MDL2 Assets"
                                                       FontSize="15"
                                                       Foreground="{DynamicResource AccentBrush}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       IsHitTestVisible="False" />
                                        </Button>
                                    </Grid>
                                </Border>

                                <Popup x:Name="PART_Popup"
                                       Placement="Bottom"
                                       PlacementTarget="{Binding ElementName=DateBorder}"
                                       AllowsTransparency="True"
                                       StaysOpen="False"
                                       PopupAnimation="Fade">
                                    <Border Background="{DynamicResource SurfaceBrush}"
                                            BorderBrush="{DynamicResource InputBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="4">
                                        <Calendar x:Name="PART_Calendar"
                                                  Background="{DynamicResource SurfaceBrush}"
                                                  Foreground="{DynamicResource TextPrimaryBrush}"
                                                  BorderThickness="0"
                                                  SelectedDate="{Binding SelectedDate, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                  DisplayDate="{Binding DisplayDate, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                  DisplayDateStart="{TemplateBinding DisplayDateStart}"
                                                  DisplayDateEnd="{TemplateBinding DisplayDateEnd}"
                                                  FirstDayOfWeek="{TemplateBinding FirstDayOfWeek}"
                                                  IsTodayHighlighted="{TemplateBinding IsTodayHighlighted}" />
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="DateBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="DateBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                    <Setter TargetName="DateBorder" Property="BorderThickness" Value="2" />
                                </Trigger>
                                <Trigger Property="Validation.HasError" Value="True">
                                    <Setter TargetName="DateBorder" Property="BorderBrush" Value="{DynamicResource OverdueBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="DateBorder" Property="Opacity" Value="0.55" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ═══ DATA GRIDS ═══
                 The framework's DataGridCell style pins Foreground to a system
                 color (near-black) and DataGridRow paints a system window
                 background. Views that themed only the row background therefore
                 rendered dark text on a dark row. Ownership of grid text color
                 lives here so every grid is legible in every theme; views that
                 declare their own DataGrid* styles still win locally. -->
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="Transparent" />
                <!-- No AlternatingRowBackground: setting it coerces AlternationCount
                     to 2 and would introduce banding in grids that never asked for it. -->
                <Setter Property="RowBackground" Value="{DynamicResource SurfaceBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource DividerBrush}" />
                <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource DividerBrush}" />
            </Style>

            <Style TargetType="DataGridCell">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="8,4" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridCell">
                            <Border Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <!-- Selection keeps the themed accent pairing rather than the
                         system highlight, so the text contrast is predictable. -->
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                        <Setter Property="Foreground" Value="{DynamicResource OnAccentBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{DynamicResource SurfaceMutedBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                <Setter Property="BorderThickness" Value="0,0,0,1" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
            </Style>

            <Style TargetType="DataGridRowHeader">
                <Setter Property="Background" Value="{DynamicResource SurfaceMutedBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            </Style>

            <Style TargetType="Separator">
                <Setter Property="Background"
                        Value="{DynamicResource DividerBrush}" />
                <Setter Property="Margin"
                        Value="0,4,0,12" />
            </Style>

            <!-- Tick boxes carry their label in the control's own Foreground, and the
                 framework default for that is black. Left alone, every checkbox and
                 radio label in the application rendered black on a dark theme's
                 background. The box and dot are retemplated for the same reason: the
                 framework paints them from system brushes no application dictionary
                 can reach, which put a stark white square on a dark surface.
                 Keyboard focus stays visible, and the checked state changes the mark
                 as well as the fill, so state is never carried by colour alone. -->
            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="Background" Value="{DynamicResource SurfaceRaisedBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Padding" Value="6,0,0,0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <Grid Background="Transparent" SnapsToDevicePixels="True">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border x:Name="Box"
                                        Width="16" Height="16"
                                        CornerRadius="3"
                                        VerticalAlignment="Center"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1">
                                    <Path x:Name="Tick"
                                          Visibility="Collapsed"
                                          Stretch="Uniform"
                                          Margin="3"
                                          StrokeThickness="2"
                                          StrokeEndLineCap="Round"
                                          StrokeStartLineCap="Round"
                                          Stroke="{DynamicResource OnAccentBrush}"
                                          Data="M 0,4 L 3.5,7.5 L 9,1" />
                                </Border>
                                <Border x:Name="FocusRing"
                                        Grid.ColumnSpan="2"
                                        CornerRadius="3"
                                        BorderThickness="1"
                                        BorderBrush="Transparent"
                                        Margin="-2,-1,-2,-1" />
                                <ContentPresenter Grid.Column="1"
                                                  Margin="{TemplateBinding Padding}"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Box" Property="Background"
                                            Value="{DynamicResource AccentBrush}" />
                                    <Setter TargetName="Box" Property="BorderBrush"
                                            Value="{DynamicResource AccentBrush}" />
                                    <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="{x:Null}">
                                    <Setter TargetName="Box" Property="BorderBrush"
                                            Value="{DynamicResource AccentBrush}" />
                                    <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
                                    <Setter TargetName="Tick" Property="Data" Value="M 0,4 L 9,4" />
                                    <Setter TargetName="Tick" Property="Stroke"
                                            Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Box" Property="BorderBrush"
                                            Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="FocusRing" Property="BorderBrush"
                                            Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground"
                                            Value="{DynamicResource TextMutedBrush}" />
                                    <Setter TargetName="Box" Property="Opacity" Value="0.55" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="RadioButton">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="Background" Value="{DynamicResource SurfaceRaisedBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Padding" Value="6,0,0,0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Grid Background="Transparent" SnapsToDevicePixels="True">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border x:Name="Ring"
                                        Width="16" Height="16"
                                        CornerRadius="8"
                                        VerticalAlignment="Center"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1">
                                    <Ellipse x:Name="Dot"
                                             Width="8" Height="8"
                                             Visibility="Collapsed"
                                             Fill="{DynamicResource AccentBrush}" />
                                </Border>
                                <Border x:Name="FocusRing"
                                        Grid.ColumnSpan="2"
                                        CornerRadius="3"
                                        BorderThickness="1"
                                        BorderBrush="Transparent"
                                        Margin="-2,-1,-2,-1" />
                                <ContentPresenter Grid.Column="1"
                                                  Margin="{TemplateBinding Padding}"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Ring" Property="BorderBrush"
                                            Value="{DynamicResource AccentBrush}" />
                                    <Setter TargetName="Dot" Property="Visibility" Value="Visible" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Ring" Property="BorderBrush"
                                            Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="FocusRing" Property="BorderBrush"
                                            Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground"
                                            Value="{DynamicResource TextMutedBrush}" />
                                    <Setter TargetName="Ring" Property="Opacity" Value="0.55" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Focus ring for elements made operable by Helpers.ClickableSurface.
                 The system dotted rectangle is nearly invisible on Sati's darker
                 palettes, so this draws the theme accent instead. -->
            <Style x:Key="ClickableSurfaceFocusVisual">
                <Setter Property="Control.Template">
                    <Setter.Value>
                        <ControlTemplate>
                            <Rectangle StrokeThickness="2"
                                       RadiusX="4"
                                       RadiusY="4"
                                       Margin="-2"
                                       SnapsToDevicePixels="True"
                                       Stroke="{DynamicResource AccentBrush}" />
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- An Expander's header text takes the control's own Foreground, which
                 the framework leaves black. Screens that group optional detail behind
                 an expander were unreadable on a dark theme for that reason alone. -->
            <Style TargetType="Expander">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
            </Style>

            <!-- Tab strips are the worst of the framework defaults on a dark theme:
                 the strip and the content panel both paint near-white, so themed
                 light text on a selected tab all but vanished. Screens with their own
                 tab styling keep it; everything else gets this. The selected tab is
                 marked by an accent rule and heavier weight as well as by fill. -->
            <Style TargetType="TabControl">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="0,10,0,0" />
            </Style>

            <Style TargetType="TabItem">
                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border x:Name="TabSurface"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="0,0,0,2"
                                    Padding="14,8"
                                    Margin="0,0,4,0">
                                <ContentPresenter ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="TabSurface" Property="Background"
                                            Value="{DynamicResource HoverBrush}" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="TabSurface" Property="Background"
                                            Value="{DynamicResource SurfaceAltBrush}" />
                                    <Setter TargetName="TabSurface" Property="BorderBrush"
                                            Value="{DynamicResource AccentBrush}" />
                                    <Setter Property="Foreground"
                                            Value="{DynamicResource TextPrimaryBrush}" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="TabSurface" Property="BorderBrush"
                                            Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground"
                                            Value="{DynamicResource TextMutedBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Context menus must use the same foreground/background contract as
                 the rest of the application. The framework defaults otherwise use
                 system menu colours, which become dark-on-dark in Sati's dark
                 themes. Selection also changes weight so it is not colour-only. -->
            <Style TargetType="ContextMenu">
                <Setter Property="Background" Value="{DynamicResource SurfaceRaisedBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="3" />
            </Style>

            <Style TargetType="MenuItem">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="Padding" Value="8,5" />
                <Style.Triggers>
                    <Trigger Property="IsHighlighted" Value="True">
                        <Setter Property="Background" Value="{DynamicResource HoverStrongBrush}" />
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                    </Trigger>
                    <Trigger Property="IsSubmenuOpen" Value="True">
                        <Setter Property="Background" Value="{DynamicResource HoverStrongBrush}" />
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
                        <Setter Property="Opacity" Value="0.65" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Base spacing for all text buttons. New button styles should use
                 BasedOn="{StaticResource ButtonBaseStyle}" so their content
                 never sits against the left and right edges. -->
            <Style x:Key="ButtonBaseStyle"
                   TargetType="Button">
                <Setter Property="Padding"
                        Value="18,6" />
            </Style>

            <Style TargetType="Button"
                   BasedOn="{StaticResource ButtonBaseStyle}" />

            <Style x:Key="PrimaryButton"
                   TargetType="Button"
                   BasedOn="{StaticResource ButtonBaseStyle}">
                <Setter Property="Height"
                        Value="36" />
                <Setter Property="Background"
                        Value="{DynamicResource AccentButtonBrush}" />
                <Setter Property="Foreground"
                        Value="{DynamicResource OnAccentButtonBrush}" />
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="FontWeight"
                        Value="SemiBold" />
                <Setter Property="BorderThickness"
                        Value="0" />
                <Setter Property="Cursor"
                        Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver"
                                         Value="True">
                                    <Setter Property="Background"
                                            Value="{DynamicResource AccentButtonHoverBrush}" />
                                </Trigger>
                                <Trigger Property="IsPressed"
                                         Value="True">
                                    <Setter Property="Background"
                                            Value="{DynamicResource AccentButtonPressedBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="LocalAiButton"
                   TargetType="Button"
                   BasedOn="{StaticResource ButtonBaseStyle}">
                <Setter Property="Height"
                        Value="34" />
                <Setter Property="Padding"
                        Value="22,0" />
                <Setter Property="Background"
                        Value="{DynamicResource AiAccentBrush}" />
                <Setter Property="Foreground"
                        Value="{DynamicResource OnAiAccentBrush}" />
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="FontWeight"
                        Value="SemiBold" />
                <Setter Property="BorderThickness"
                        Value="0" />
                <Setter Property="Cursor"
                        Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver"
                                         Value="True">
                                    <Setter Property="Background"
                                            Value="{DynamicResource AiAccentHoverBrush}" />
                                </Trigger>
                                <Trigger Property="IsPressed"
                                         Value="True">
                                    <Setter Property="Background"
                                            Value="{DynamicResource AiAccentPressedBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled"
                                         Value="False">
                                    <Setter Property="Opacity"
                                            Value="0.55" />
                                    <Setter Property="Cursor"
                                            Value="Arrow" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Destructive actions take the danger fill and the ink named for it.
                 They previously borrowed BorderBrush and AccentPressedBrush, which
                 are a boundary colour and a pressed-surface fill; on a dark theme
                 that put a dim accent on a dim grey and the label all but vanished. -->
            <Style x:Key="DangerButton"
                   TargetType="Button"
                   BasedOn="{StaticResource ButtonBaseStyle}">
                <Setter Property="Height"
                        Value="36" />
                <Setter Property="Background"
                        Value="{DynamicResource DangerStrongBrush}" />
                <Setter Property="Foreground"
                        Value="{DynamicResource OnDangerStrongBrush}" />
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="FontWeight"
                        Value="SemiBold" />
                <Setter Property="BorderThickness"
                        Value="0" />
                <Setter Property="Cursor"
                        Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver"
                                         Value="True">
                                    <Setter Property="Background"
                                            Value="{DynamicResource DangerDarkBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="IconButton"
                   TargetType="Button"
                   BasedOn="{StaticResource ButtonBaseStyle}">
                <Setter Property="Width"
                        Value="32" />
                <Setter Property="Height"
                        Value="32" />
                <Setter Property="Padding"
                        Value="0" />
                <Setter Property="Background"
                        Value="Transparent" />
                <Setter Property="BorderThickness"
                        Value="0" />
                <Setter Property="Cursor"
                        Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver"
                                         Value="True">
                                    <Setter Property="Background"
                                            Value="{DynamicResource BorderBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="SmallButton"
                   TargetType="Button"
                   BasedOn="{StaticResource ButtonBaseStyle}">
                <Setter Property="Height"
                        Value="28" />
                <Setter Property="Padding"
                        Value="12,0" />
                <Setter Property="Background"
                        Value="{DynamicResource HoverBrush}" />
                <Setter Property="Foreground"
                        Value="{DynamicResource TextPrimaryBrush}" />
                <Setter Property="FontSize"
                        Value="12" />
                <Setter Property="BorderThickness"
                        Value="0" />
                <Setter Property="Cursor"
                        Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver"
                                         Value="True">
                                    <Setter Property="Background"
                                            Value="{DynamicResource HoverStrongBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============================================================ -->
            <!-- NAVIGATION TABS                                              -->
            <!--                                                              -->
            <!-- THE one owner of what a navigation tab looks like, for the    -->
            <!-- shell's top bar and for the sub-navigation inside a section.  -->
            <!-- Selection state arrives through views:NavTab.IsActive, so a   -->
            <!-- tab binds its own view model property and this style decides  -->
            <!-- how selection is rendered. Change it here, once.              -->
            <!--                                                              -->
            <!-- Selection is shown with FOREGROUND COLOUR AND FONT WEIGHT,    -->
            <!-- never a filled background. Two reasons: a filled block forces -->
            <!-- the label to invert to the window background colour, which is -->
            <!-- where the dark-on-dark legibility bugs kept coming from; and  -->
            <!-- weight is a non-colour cue, so selection survives for a user  -->
            <!-- who cannot distinguish the accent hue. Hover and keyboard     -->
            <!-- focus still use background and border, because those are      -->
            <!-- transient states rather than status.                          -->
            <!-- ============================================================ -->
            <Style x:Key="NavTabButton"
                   TargetType="Button">
                <Setter Property="Foreground"
                        Value="{DynamicResource TextSecondaryBrush}" />
                <Setter Property="Background"
                        Value="Transparent" />
                <Setter Property="FontSize"
                        Value="13" />
                <Setter Property="FontWeight"
                        Value="Normal" />
                <Setter Property="Padding"
                        Value="10,4" />
                <Setter Property="Margin"
                        Value="0,0,4,0" />
                <Setter Property="Cursor"
                        Value="Hand" />
                <Setter Property="HorizontalContentAlignment"
                        Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="TabSurface"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="Transparent"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="{TemplateBinding Padding}"
                                    SnapsToDevicePixels="True">
                                <Grid>
                                    <!-- Width reservation. Selecting a tab makes its
                                         label bold, and bold text is wider, so without
                                         this every click would shove the rest of the
                                         navigation bar sideways. This hidden copy is
                                         always bold, so the tab is always as wide as
                                         its widest state. Assumes string content,
                                         which every navigation tab uses. -->
                                    <TextBlock Text="{TemplateBinding Content}"
                                               FontWeight="SemiBold"
                                               Visibility="Hidden"
                                               IsHitTestVisible="False" />
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver"
                                         Value="True">
                                    <Setter TargetName="TabSurface"
                                            Property="Background"
                                            Value="{DynamicResource HoverBrush}" />
                                </Trigger>
                                <!-- Keyboard focus must be visible, not only hover. -->
                                <Trigger Property="IsKeyboardFocused"
                                         Value="True">
                                    <Setter TargetName="TabSurface"
                                            Property="BorderBrush"
                                            Value="{DynamicResource AccentBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="views:NavTab.IsActive"
                             Value="True">
                        <Setter Property="Foreground"
                                Value="{DynamicResource AccentBrush}" />
                        <Setter Property="FontWeight"
                                Value="SemiBold" />
                        <!-- Announced to screen readers, which cannot see either
                             the colour or the weight. -->
                        <Setter Property="AutomationProperties.ItemStatus"
                                Value="Selected" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Converters -->
            <converters:PngBytesToImageConverter x:Key="PngBytesToImageConverter" />
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
            <converters:BillingPeriodLabelConverter x:Key="BillingPeriodLabelConverter" />
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
            <converters:EnumToBoolConverter x:Key="EnumToBoolConverter" />
            <converters:BoardTabConverter x:Key="BoardTabConverter" />
            <converters:EnumDescriptionConverter x:Key="EnumDescriptionConverter" />
            <converters:BoolToTileColorConverter x:Key="BoolToTileColorConverter" />
            <converters:BoolToWidthConverter x:Key="BoolToWidthConverter" />
            <converters:BoolToWidthConverter x:Key="SpacerWidthConverter"
                                             TrueWidth="16"
                                             FalseWidth="0" />
            <converters:FormCellStatusToBrushConverter x:Key="FormCellStatusToBrushConverter" />
            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
            <converters:CreatedAtLabelConverter x:Key="CreatedAtLabelConverter" />
            <converters:PersonNameFormatConverter x:Key="PersonNameFormatConverter" />

            <!-- Service-day bar: position and length as fractions of the measured
                 track. The width instance keeps a floor so a five-minute contact
                 is still visible. -->
            <converters:TimelineFractionConverter x:Key="TimelineWidthConverter"
                                                  MinimumPixels="3" />
            <converters:TimelineOffsetMarginConverter x:Key="TimelineOffsetMarginConverter" />
            <!-- View → ViewModel DataTemplates -->
            <DataTemplate DataType="{x:Type viewmodels:CaseManagerDashboardViewModel}">
                <views:CaseManagerDashboardView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type children:NoteEntryViewModel}">
                <views:NoteEntryView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type supervisorVms:SupervisorDashboardViewModel}">
                <views:SupervisorDashboardWindow />
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:NotesWindowViewModel}">
                <views:NotesLogView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:StatisticsViewModel}">
                <views:StatisticsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:NewClientViewModel}">
                <views:ClientsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:CaseloadMatrixViewModel}">
                <views:CaseloadMatrixView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodels:ReviewsViewModel}">
                <views:ReviewsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type children:GuidanceViewModel}">
                <views:GuidanceView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type children:CalendarViewModel}">
                <views:CalendarView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type children:CaseManagementViewModel}">
                <views:CaseManagementView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type children:ChatViewModel}">
                <views:ChatPanelView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type children:HelperReferenceViewModel}">
                <views:HelperReferenceView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type children:ATRequestViewModel}">
                <views:ATRequestView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type clientDocumentVms:ClientDocumentHubViewModel}">
                <clientDocumentViews:ClientDocumentHubView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type children:ProvidersViewModel}">
                <views:ProvidersView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type children:ConsumerImportViewModel}">
                <views:ConsumerImportView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type supervisorVms:TeamOverviewViewModel}">
                <views:TeamOverviewView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type supervisorVms:OverdueItemsViewModel}">
                <views:OverdueItemsView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type supervisorVms:MonthlyProductivityViewModel}">
                <views:MonthlyProductivityView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type supervisorVms:UserManagementViewModel}">
                <views:UserManagementView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type supervisorVms:PendingApprovalsViewModel}">
                <views:PendingApprovalsView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type supervisorVms:CaseloadDistributionViewModel}">
                <views:CaseloadDistributionView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type supervisorVms:CaseloadImportViewModel}">
                <views:CaseloadImportView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type adminVms:AdminDashboardViewModel}">
                <views:AdminDashboardView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type adminVms:PlatformHealthViewModel}">
                <views:PlatformHealthView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type billingVms:BillingDashboardViewModel}">
                <billingViews:BillingDashboardView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type billingVms:BillingOverviewViewModel}">
                <billingViews:BillingOverviewView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type billingVms:BillingQueueViewModel}">
                <billingViews:BillingQueueView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type billingVms:BillingSubmissionsViewModel}">
                <billingViews:BillingSubmissionsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type billingVms:BillingRemittancesViewModel}">
                <billingViews:BillingRemittancesView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type billingVms:BillingAlertsViewModel}">
                <billingViews:BillingAlertsView />
            </DataTemplate>

        </ResourceDictionary>
    </Application.Resources>
</Application>
