<UserControl x:Class="Sati.Views.MonthlyProductivityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:oxy="http://oxyplot.org/wpf"
             Background="{DynamicResource WindowBackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24,20,24,32">

            <!-- ── Team status totals ── -->
            <TextBlock Text="TEAM TOTALS THIS MONTH"
                       FontFamily="Inter"
                       FontSize="11"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextMutedBrush}"
                       Margin="0,0,0,8" />
            <UniformGrid Columns="6"
                         Margin="0,0,0,24">
                <Border Margin="0,0,4,0"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource HoverBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="12,10">
                    <StackPanel>
                        <TextBlock Text="{Binding TeamLogged}"
                                   FontFamily="Inter"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource CompliantBrush}" />
                        <TextBlock Text="Logged"
                                   FontFamily="Inter"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}" />
                    </StackPanel>
                </Border>
                <Border Margin="4,0"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource HoverBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="12,10">
                    <StackPanel>
                        <TextBlock Text="{Binding TeamScheduled}"
                                   FontFamily="Inter"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource InfoBrush}" />
                        <TextBlock Text="Scheduled"
                                   FontFamily="Inter"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}" />
                    </StackPanel>
                </Border>
                <Border Margin="4,0"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource HoverBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="12,10">
                    <StackPanel>
                        <TextBlock Text="{Binding TeamPending}"
                                   FontFamily="Inter"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource AccentBrush}" />
                        <TextBlock Text="Pending"
                                   FontFamily="Inter"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}" />
                    </StackPanel>
                </Border>
                <Border Margin="4,0"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource HoverBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="12,10">
                    <StackPanel>
                        <TextBlock Text="{Binding TeamDelayed}"
                                   FontFamily="Inter"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource AccentBrush}" />
                        <TextBlock Text="Delayed"
                                   FontFamily="Inter"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}" />
                    </StackPanel>
                </Border>
                <Border Margin="4,0"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource HoverBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="12,10">
                    <StackPanel>
                        <TextBlock Text="{Binding TeamCancelled}"
                                   FontFamily="Inter"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource TextMutedBrush}" />
                        <TextBlock Text="Cancelled"
                                   FontFamily="Inter"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}" />
                    </StackPanel>
                </Border>
                <Border Margin="4,0,0,0"
                        Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource HoverBrush}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="12,10">
                    <StackPanel>
                        <TextBlock Text="{Binding TeamAbandoned}"
                                   FontFamily="Inter"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource WorkflowPurpleBrush}" />
                        <TextBlock Text="Abandoned"
                                   FontFamily="Inter"
                                   FontSize="11"
                                   Foreground="{DynamicResource TextMutedBrush}" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- ── Per-CM breakdown table ── -->
            <TextBlock Text="BY CASE MANAGER"
                       FontFamily="Inter"
                       FontSize="11"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextMutedBrush}"
                       Margin="0,0,0,8" />
            <DataGrid AutomationProperties.Name="Case managers" ItemsSource="{Binding CaseManagers}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="False"
                      CanUserResizeRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      SelectionUnit="FullRow"
                      SelectionMode="Single"
                      Background="Transparent"
                      BorderThickness="0"
                      RowBackground="Transparent"
                      AlternatingRowBackground="{DynamicResource SurfaceRaisedBrush}"
                      HorizontalGridLinesBrush="{DynamicResource HoverBrush}"
                      Margin="0,0,0,28">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background"
                                Value="Transparent" />
                        <Setter Property="Foreground"
                                Value="{DynamicResource TextMutedBrush}" />
                        <Setter Property="FontFamily"
                                Value="Inter" />
                        <Setter Property="FontSize"
                                Value="11" />
                        <Setter Property="FontWeight"
                                Value="SemiBold" />
                        <Setter Property="Padding"
                                Value="8,8" />
                        <Setter Property="BorderThickness"
                                Value="0,0,0,1" />
                        <Setter Property="BorderBrush"
                                Value="{DynamicResource HoverBrush}" />
                        <Setter Property="SeparatorVisibility"
                                Value="Collapsed" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness"
                                Value="0" />
                        <Setter Property="Padding"
                                Value="8,6" />
                        <Setter Property="Foreground"
                                Value="{DynamicResource TextPrimaryBrush}" />
                        <Setter Property="FontFamily"
                                Value="Inter" />
                        <Setter Property="Background"
                                Value="Transparent" />
                        <Setter Property="FocusVisualStyle"
                                Value="{x:Null}" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="{DynamicResource HoverBrush}" />
                                <Setter Property="Foreground"
                                        Value="{DynamicResource TextPrimaryBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderThickness"
                                Value="0" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name"
                                        Binding="{Binding DisplayName}"
                                        Width="*" />
                    <DataGridTextColumn Header="Logged"
                                        Binding="{Binding LoggedCount}"
                                        Width="70" />
                    <DataGridTextColumn Header="Sched."
                                        Binding="{Binding ScheduledCount}"
                                        Width="60" />
                    <DataGridTextColumn Header="Pending"
                                        Binding="{Binding PendingCount}"
                                        Width="70" />
                    <DataGridTextColumn Header="Delayed"
                                        Binding="{Binding DelayedCount}"
                                        Width="70" />
                    <DataGridTextColumn Header="Cancelled"
                                        Binding="{Binding CancelledCount}"
                                        Width="80" />
                    <DataGridTextColumn Header="Abandoned"
                                        Binding="{Binding AbandonedCount}"
                                        Width="80" />
                    <!-- Days the case manager marked as producing nothing billable, shown only
                         once their documentation window has closed: before that they can still
                         write the day up, and asking would be premature. -->
                    <DataGridTemplateColumn Header="No billable work"
                                            Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Margin="8,6"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           Text="{Binding SettledDaysWithoutBillableWork.Count}"
                                           ToolTip="{Binding SettledDaysWithoutBillableWorkDetail}"
                                           AutomationProperties.Name="{Binding SettledDaysWithoutBillableWorkLabel}"
                                           Visibility="{Binding HasSettledDaysWithoutBillableWork,
                                               Converter={StaticResource BoolToVisibilityConverter}}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Progress"
                                            Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ProgressBar Value="{Binding ProgressPercent, Mode=OneWay}"
                                             Maximum="100"
                                             Height="8"
                                             Margin="8,6"
                                             BorderThickness="0">
                                    <ProgressBar.Style>
                                        <Style TargetType="ProgressBar">
                                            <Setter Property="Foreground"
                                                    Value="{DynamicResource AccentBrush}" />
                                            <Setter Property="Background"
                                                    Value="{DynamicResource HoverBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StatusLevel}"
                                                             Value="Ok">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource CompliantBrush}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StatusLevel}"
                                                             Value="Danger">
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource WorkflowPurpleBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ProgressBar.Style>
                                </ProgressBar>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="%"
                                        Binding="{Binding ProgressPercent, StringFormat={}{0:0}%}"
                                        Width="50" />
                </DataGrid.Columns>
            </DataGrid>

            <!-- ── Stacked status chart ── -->
            <TextBlock Text="NOTE STATUS BY CASE MANAGER"
                       FontFamily="Inter"
                       FontSize="11"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextMutedBrush}"
                       Margin="0,0,0,8" />
            <Border Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource HoverBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="12">
                <oxy:PlotView Name="StatusChart"
                    Model="{Binding StatusChartModel}"
                              Height="340"
                              Background="Transparent" />
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>