<UserControl x:Class="Sati.Views.ClientsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sati;assembly=Sati.Persistence"
        xmlns:converters="clr-namespace:Sati.Converters"
        xmlns:helpers="clr-namespace:Sati.Helpers"
        xmlns:clientDocuments="clr-namespace:Sati.Views.ClientDocuments"
        xmlns:views="clr-namespace:Sati.Views"
        Background="{DynamicResource WindowBackgroundBrush}">

    <UserControl.Resources>
        <helpers:BindingProxy x:Key="LayoutProxy" Data="{Binding}" />
        <converters:BoolToWidthConverter x:Key="BoolToWidthConverter" />
        <converters:BoolToWidthConverter x:Key="SpacerWidthConverter"
                                         TrueWidth="16"
                                         FalseWidth="0" />
        <converters:BoolToWidthConverter x:Key="CompactWorkspaceRailWidthConverter"
                                         TrueWidth="140"
                                         FalseWidth="180" />
        <converters:BoolToWidthConverter x:Key="CompactWorkspaceSpacerWidthConverter"
                                         TrueWidth="8"
                                         FalseWidth="16" />
        <converters:PersonBillingComplianceConverter x:Key="PersonBillingComplianceConverter" />
        <!-- Dot-grid fill for the Journal. A DrawingBrush tiling a single 2px dot in
             the palette's border tone at a 16px pitch. Viewport is absolute (Viewport
             + ViewportUnits=Absolute), so the dot stays a fixed size and the grid
             density doesn't stretch with the box. TileMode=Tile repeats it. Faint by
             color choice, not opacity, so it doesn't dim text drawn over it. -->
        <DrawingBrush x:Key="JournalDotBrush"
                      TileMode="Tile"
                      Viewport="0,0,16,16"
                      ViewportUnits="Absolute"
                      Stretch="None">
            <DrawingBrush.Drawing>
                <GeometryDrawing Brush="{DynamicResource BorderBrush}">
                    <GeometryDrawing.Geometry>
                        <EllipseGeometry Center="8,8"
                                         RadiusX="1"
                                         RadiusY="1" />
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingBrush.Drawing>
        </DrawingBrush>

        <!-- The Add Client form's action row. Given an explicit height because the row also
             holds the "required to save" panel, and a stretched StackPanel would otherwise size
             every button to that panel's height — which is what made three buttons read as a
             wall rather than a toolbar. -->
        <Style x:Key="FormActionButton"
               TargetType="Button">
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        </Style>

        <!-- The one button in the row that writes a record. It is the only one carrying the
             accent, so the two beside it read as the alternatives they are. -->
        <Style x:Key="FormActionPrimaryButton"
               TargetType="Button"
               BasedOn="{StaticResource FormActionButton}">
            <Setter Property="Margin" Value="0" />
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource OnAccentBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="LabelStyle"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="11" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="Foreground"
                    Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="Margin"
                    Value="0,0,0,5" />
        </Style>

        <Style x:Key="SectionHeader"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="13" />
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="Foreground"
                    Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Margin"
                    Value="0,0,0,8" />
        </Style>

        <Style x:Key="CompactClientPanelPadding" TargetType="Border">
            <Setter Property="Padding" Value="16" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCompactDisplayMode}" Value="True">
                    <Setter Property="Padding" Value="6" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SsnFieldStyle" TargetType="TextBox">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="FieldBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="FieldBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SsnActionButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource ButtonBaseStyle}">
            <Setter Property="Height" Value="36" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource HoverStrongBrush}" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Tag" Value="Primary" />
                                    <Condition Property="IsMouseOver" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource AccentHoverBrush}" />
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Tag" Value="Primary" />
                                    <Condition Property="IsPressed" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource AccentPressedBrush}" />
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SsnPrimaryButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource SsnActionButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource OnAccentBrush}" />
            <Setter Property="Tag" Value="Primary" />
        </Style>

        <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="Foreground"
                    Value="{DynamicResource TextPrimaryBrush}" />
            <Setter Property="Margin"
                    Value="0,0,0,6" />
        </Style>


        <Style x:Key="IconButton"
               TargetType="Button">
            <Setter Property="Width"
                    Value="32" />
            <Setter Property="Height"
                    Value="32" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="{DynamicResource BorderBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- One code-native icon is shared by the full roster and the compact
             selector. The silhouette is neutral; the plus follows the active
             theme accent so it reads as an action rather than a refresh glyph. -->
        <DataTemplate x:Key="AddClientIconTemplate">
            <Viewbox Width="20" Height="20">
                <Canvas Width="24" Height="24">
                    <Ellipse Width="7"
                             Height="7"
                             Canvas.Left="4.5"
                             Canvas.Top="2"
                             Fill="{DynamicResource TextSecondaryBrush}" />
                    <Path Data="M2,20 C2,14.6 5.1,11.5 8,11.5 C10.9,11.5 14,14.6 14,20 Z"
                          Fill="{DynamicResource TextSecondaryBrush}" />
                    <Ellipse Width="10"
                             Height="10"
                             Canvas.Left="13"
                             Canvas.Top="12"
                             Fill="{DynamicResource SurfaceBrush}"
                             Stroke="{DynamicResource AccentBrush}"
                             StrokeThickness="1.5" />
                    <Path Data="M18,14.5 L18,19.5 M15.5,17 L20.5,17"
                          Stroke="{DynamicResource AccentBrush}"
                          StrokeThickness="1.8"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round" />
                </Canvas>
            </Viewbox>
        </DataTemplate>

        <!-- Client document navigation. The identity rail remains outside this
             control so changing workspaces never loses consumer context. -->
        <Style x:Key="ClientWorkspaceTabItemStyle"
               TargetType="TabItem">
            <Setter Property="Foreground"
                    Value="{DynamicResource TextSecondaryBrush}" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="FontSize"
                    Value="12" />
            <Setter Property="Padding"
                    Value="12,10" />
            <Setter Property="Margin"
                    Value="0,0,0,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="WorkspaceTabBackground"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Left" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="WorkspaceTabBackground"
                                        Property="Background"
                                        Value="{DynamicResource HoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected"
                                     Value="True">
                                <Setter TargetName="WorkspaceTabBackground"
                                        Property="Background"
                                        Value="{DynamicResource SurfaceBrush}" />
                                <Setter Property="Foreground"
                                        Value="{DynamicResource AccentPressedBrush}" />
                                <Setter Property="FontWeight"
                                        Value="SemiBold" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsCompactDisplayMode}" Value="True">
                    <Setter Property="Padding" Value="8,6" />
                    <Setter Property="Margin" Value="0,0,0,2" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ClientWorkspaceTabControlStyle"
               TargetType="TabControl">
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <StackPanel IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="{Binding IsCompactDisplayMode,
                                    Converter={StaticResource CompactWorkspaceRailWidthConverter}}" />
                                <ColumnDefinition Width="{Binding IsCompactDisplayMode,
                                    Converter={StaticResource CompactWorkspaceSpacerWidthConverter}}" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Background="{DynamicResource SurfaceAltBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Padding" Value="10" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCompactDisplayMode}" Value="True">
                                                <Setter Property="Padding" Value="6" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="CONSUMER RECORD"
                                               FontSize="11"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextMutedBrush}"
                                               Margin="10,4,0,12" />
                                    <ScrollViewer Grid.Row="1"
                                                  VerticalScrollBarVisibility="Visible"
                                                  HorizontalScrollBarVisibility="Disabled"
                                                  AutomationProperties.Name="Consumer record section navigation">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Grid>
                            </Border>

                            <ContentPresenter Grid.Column="2"
                                              ContentSource="SelectedContent" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.Style>
            <Style TargetType="Grid">
                <Setter Property="Margin" Value="16" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsCompactDisplayMode}" Value="True">
                        <Setter Property="Margin" Value="4" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0" />
            <ColumnDefinition Width="0" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ═══ ENTRY FORM (left) ═══ -->
        <Border Grid.Column="0"
                Style="{StaticResource CompactClientPanelPadding}"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Visibility="{Binding IsEntryPanelOpen, 
  Converter={StaticResource BoolToVisibilityConverter}}">
            <ScrollViewer VerticalScrollBarVisibility="Visible"
                          HorizontalScrollBarVisibility="Auto"
                          AutomationProperties.Name="Consumer entry form">
                <StackPanel>
                    <TextBlock Text="{Binding EntryPanelHeader}"
                               FontSize="13"
                               FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}"
                           Margin="0,0,0,16" />

                <TextBlock Style="{StaticResource LabelStyle}">
                    <Run Text="FIRST NAME" /><Run Text=" *" Foreground="{DynamicResource WarningBrush}" />
                </TextBlock>
                <TextBox AutomationProperties.Name="First Name, required" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Style="{StaticResource LabelStyle}">
                    <Run Text="LAST NAME" /><Run Text=" *" Foreground="{DynamicResource WarningBrush}" />
                </TextBlock>
                <TextBox AutomationProperties.Name="Last Name, required" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Style="{StaticResource LabelStyle}">
                    <Run Text="BIRTHDAY" /><Run Text=" *" Foreground="{DynamicResource WarningBrush}" />
                </TextBlock>
                <DatePicker AutomationProperties.Name="Date of birth, required" SelectedDate="{Binding BirthDate, Mode=TwoWay}" />

                <TextBlock Text="GENDER / PRONOUNS"
                           Style="{StaticResource LabelStyle}" />
                <ComboBox AutomationProperties.Name="Gender and pronouns" ItemsSource="{Binding Genders}"
                          SelectedItem="{Binding Gender, Mode=TwoWay}" />

                <TextBlock Text="WAIVER"
                           Style="{StaticResource LabelStyle}" />
                <ComboBox AutomationProperties.Name="Waiver" ItemsSource="{Binding Waivers}"
                          SelectedItem="{Binding Waiver, Mode=TwoWay}" />

                <TextBlock Text="EFFECTIVE DATE"
                           Style="{StaticResource LabelStyle}" />
                <Grid Margin="0,0,0,10"
                      IsEnabled="{Binding HasWaiver}">
                    <TextBox AutomationProperties.Name="Effective date" Text="{Binding EffectiveDateText, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="MM/DD"
                               Foreground="{DynamicResource InputBorderBrush}"
                               FontSize="13"
                               Margin="9,4"
                               IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility"
                                        Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding EffectiveDateText}"
                                                 Value="">
                                        <Setter Property="Visibility"
                                                Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    </Grid>

                    <TextBlock Style="{StaticResource LabelStyle}">
                        <Run Text="BIO" /><Run Text=" *" Foreground="{DynamicResource WarningBrush}" />
                    </TextBlock>
                    <TextBox AutomationProperties.Name="Biography, required" Text="{Binding Bio, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         Height="100" />

                    <TextBlock Text="EVERGREEN ID"
                               Style="{StaticResource LabelStyle}" />
                    <TextBox AutomationProperties.Name="Evergreen " Text="{Binding EvergreenId, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Text="PHONE"
                               Style="{StaticResource LabelStyle}" />
                    <TextBox AutomationProperties.Name="Phone Number" Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="EMAIL (OPTIONAL)"
                               Style="{StaticResource LabelStyle}" />
                    <TextBox AutomationProperties.Name="Consumer Email"
                             Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             AutomationProperties.HelpText="Optional. Leave blank when no email address is available."
                             MaxLength="254" />
                    <TextBlock Text="ADDRESS"
                           Style="{StaticResource LabelStyle}" />
                <TextBox AutomationProperties.Name="Address" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="PRIMARY CARE PROVIDER"
                           Style="{StaticResource LabelStyle}" />
                <TextBox AutomationProperties.Name="Primary Care Provider" Text="{Binding PrimaryCareProvider, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="HEALTHCARE SYSTEM"
                           Style="{StaticResource LabelStyle}" />
                <ComboBox AutomationProperties.Name="Healthcare System Name" ItemsSource="{Binding HealthcareSystems}"
                          DisplayMemberPath="Name"
                          SelectedValuePath="Name"
                          SelectedValue="{Binding HealthcareSystemName, Mode=TwoWay}" />

                <CheckBox Content="Case manager is DHHS representative"
                          IsChecked="{Binding CaseManagerIsDhhsRepresentative}" />
                <CheckBox Content="Uses Modivcare"
                          IsChecked="{Binding UsesModivcare}" />

                <CheckBox Content="Open with VR"
                          IsChecked="{Binding OpenWithVR}"
                          Margin="0,12,0,6" />
                <StackPanel Visibility="{Binding OpenWithVR, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="VOCATIONAL REHABILITATION COUNSELOR (VRC)"
                               Style="{StaticResource LabelStyle}" />
                    <TextBox Text="{Binding VrCounselorName, UpdateSourceTrigger=PropertyChanged}"
                             MaxLength="150"
                             AutomationProperties.Name="Vocational Rehabilitation Counselor" />
                    <TextBlock Text="{Binding VrAssistantTitle}"
                               Style="{StaticResource LabelStyle}" />
                    <TextBox Text="{Binding VrAssistantName, UpdateSourceTrigger=PropertyChanged}"
                             MaxLength="150"
                             AutomationProperties.Name="Vocational Rehabilitation assistant assignment" />
                </StackPanel>

                    <CheckBox Content="Has guardian"
                              IsChecked="{Binding HasGuardian}" />
                    <TextBox AutomationProperties.Name="Guardian Name" Text="{Binding GuardianName, UpdateSourceTrigger=PropertyChanged}"
                             Visibility="{Binding HasGuardian, Converter={StaticResource BoolToVisibilityConverter}}" />

                    <TextBlock Text="WAIVER SERVICES"
                               Style="{StaticResource LabelStyle}"
                               Margin="0,16,0,6" />

                    <CheckBox Content="Home support"
                              IsChecked="{Binding HasHomeSupport}" />
                    <CheckBox Content="Self-directed home support"
                              IsChecked="{Binding HasSelfDirectedHomeSupport}" />
                    <CheckBox Content="Shared living"
                              IsChecked="{Binding HasSharedLiving}" />
                    <CheckBox Content="Community support 1:1"
                              IsChecked="{Binding HasCommunitySupport1To1}" />
                    <CheckBox Content="Community support self-directed"
                              IsChecked="{Binding HasCommunitySupportSelfDirected}" />
                    <CheckBox Content="Day program"
                              IsChecked="{Binding HasCommunitySupportDayProgram}" />

                    <!-- Reveal pattern borrowed from HasGuardian/GuardianName. Bound
                     LostFocus rather than PropertyChanged: the VM clamps values
                     below 1 up to 1, and clamping on every keystroke makes the
                     field impossible to clear and retype. -->
                    <StackPanel Orientation="Horizontal"
                                Margin="20,0,0,6"
                                Visibility="{Binding HasCommunitySupportDayProgram, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="How many?"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <TextBox AutomationProperties.Name="Day Program Count" Text="{Binding DayProgramCount, UpdateSourceTrigger=LostFocus}"
                                 Width="48" />
                    </StackPanel>

                    <CheckBox Content="Employed"
                              IsChecked="{Binding IsEmployed}"
                              Margin="0,12,0,6" />

                    <CheckBox Content="Employment specialist"
                              IsChecked="{Binding HasEmploymentSpecialist}"
                              IsEnabled="{Binding IsEmployed}"
                              Margin="20,0,0,6" />
                    <CheckBox Content="Work supports"
                              IsChecked="{Binding HasWorkSupports}"
                              IsEnabled="{Binding IsEmployed}"
                              Margin="20,0,0,6" />

                <Border Background="{DynamicResource WarningFillBrush}"
                        BorderBrush="{DynamicResource WarningBrush}"
                        BorderThickness="1"
                        CornerRadius="5"
                        Padding="10"
                        Margin="0,8,0,10"
                        Visibility="{Binding CanMarkNewConsumerAsTest, Converter={StaticResource BoolToVisibilityConverter}}"
                        AutomationProperties.Name="Test consumer creation option">
                    <StackPanel>
                        <CheckBox Content="Test consumer"
                                  IsChecked="{Binding IsTestData}"
                                  FontWeight="SemiBold"
                                  AutomationProperties.Name="Mark new consumer as Test" />
                        <TextBlock Text="Use only for synthetic records created for testing. This designation cannot be added or removed after creation."
                                   Margin="22,3,0,0"
                                   FontSize="10.5"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource WarningFillBrush}"
                        BorderBrush="{DynamicResource WarningBrush}"
                        BorderThickness="1"
                        CornerRadius="5"
                        Padding="10"
                        Margin="0,4,0,10"
                        Visibility="{Binding HasClientSaveError, Converter={StaticResource BoolToVisibilityConverter}}"
                        AutomationProperties.LiveSetting="Assertive"
                        AutomationProperties.Name="Client save problem">
                    <TextBlock Text="{Binding ClientSaveErrorMessage}"
                               Foreground="{DynamicResource WarningBrush}"
                               TextWrapping="Wrap" />
                </Border>

                <Button Content="{Binding SubmitButtonLabel}"
                        AutomationProperties.Name="{Binding SubmitButtonLabel}"
                            Style="{StaticResource PrimaryButton}"
                        Command="{Binding SubmitCommand}"
                            Margin="0,0,0,10"/>


            </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Chevron rail — always visible; toggles the entry panel. Mirrors the
             Today's Work rail in ShellWindow, but sits to the right of its panel
             rather than the left, so the chevron directions are reversed: the arrow
             points the way the panel's outer edge will move. -->
        <Border Grid.Column="1"
                BorderThickness="1,0,1,0"
                Margin="0,0,8,0">
            <Button Command="{Binding ToggleEntryPanelCommand}"
                    AutomationProperties.Name="Show or hide client form"
                    Width="24"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    VerticalAlignment="Center"
                    ToolTip="Show or hide the client form">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <TextBlock FontFamily="Segoe MDL2 Assets"
                           FontSize="14"
                           Foreground="{DynamicResource TextSecondaryBrush}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <!-- Closed: points right = expand outward from the edge -->
                            <Setter Property="Text"
                                    Value="&#xE76C;" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEntryPanelOpen}"
                                             Value="True">
                                    <!-- Open: points left = collapse toward the edge -->
                                    <Setter Property="Text"
                                            Value="&#xE76B;" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Button>
        </Border>

        <!-- ═══ RIGHT PANEL ═══ -->
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition>
                    <ColumnDefinition.Style>
                        <Style TargetType="ColumnDefinition">
                            <Setter Property="Width" Value="250" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding UseHorizontalClientSelector}" Value="True">
                                    <Setter Property="Width" Value="0" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ColumnDefinition.Style>
                </ColumnDefinition>
                <ColumnDefinition>
                    <ColumnDefinition.Style>
                        <Style TargetType="ColumnDefinition">
                            <Setter Property="Width" Value="12" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding UseHorizontalClientSelector}" Value="True">
                                    <Setter Property="Width" Value="0" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ColumnDefinition.Style>
                </ColumnDefinition>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- ═══ CLIENT ROSTER ═══ -->
            <Border Grid.Column="0"
                    Grid.RowSpan="2"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Visibility="{Binding UseHorizontalClientSelector, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="14,12,10,10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Row="0">
                            <TextBlock Text="CLIENTS"
                                       FontSize="13"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource TextPrimaryBrush}" />
                            <TextBlock Text="{Binding People.Count, StringFormat={}{0} people}"
                                       FontSize="11"
                                       Foreground="{DynamicResource TextMutedBrush}"
                                       Margin="0,2,0,0" />
                        </StackPanel>
                        <ComboBox Grid.Row="1"
                                  Grid.Column="0"
                                  Grid.ColumnSpan="2"
                                  Margin="0,10,0,0"
                                  ItemsSource="{Binding ConsumerFilters}"
                                  SelectedItem="{Binding SelectedConsumerFilter, Mode=TwoWay}"
                                  AutomationProperties.Name="Filter consumers"
                                  ToolTip="Show consumers with the selected support or responsibility" />
                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                            <Button Style="{StaticResource IconButton}"
                                    AutomationProperties.Name="Collapse client list"
                                    Command="{Binding ToggleClientListCommand}"
                                    IsEnabled="{Binding CanToggleClientList}"
                                    ToolTip="Use compact client selector">
                                <TextBlock Text="&#xE76B;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                           Foreground="{DynamicResource TextSecondaryBrush}" />
                            </Button>
                            <Button Style="{StaticResource IconButton}"
                                    AutomationProperties.Name="Add a new client"
                                    Command="{Binding OpenEntryPanelCommand}"
                                    ToolTip="Add a new client"
                                    Content="Add client"
                                    ContentTemplate="{StaticResource AddClientIconTemplate}" />
                        </StackPanel>
                    </Grid>

                    <ListBox AutomationProperties.Name="Clients"
                             Grid.Row="1"
                             ItemsSource="{Binding PeopleView}"
                             SelectedItem="{Binding SelectedPerson, Mode=TwoWay}"
                             MouseDoubleClick="ClientList_MouseDoubleClick"
                             IsEnabled="{Binding IsClientEditorOpen, Converter={StaticResource InverseBoolConverter}}"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.VerticalScrollBarVisibility="Visible"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             Padding="6,0,6,8">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0,0,0,4" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="ClientCard"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="Transparent"
                                                    BorderThickness="1"
                                                    CornerRadius="6"
                                                    Padding="10,7.2">
                                                <ContentPresenter />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ClientCard" Property="Background" Value="{DynamicResource HoverBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                                    <Setter TargetName="ClientCard" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                                </Trigger>
                                                 <Trigger Property="IsSelected" Value="True">
                                                     <Setter TargetName="ClientCard" Property="Background" Value="{DynamicResource PersonGroupBrush}" />
                                                     <Setter TargetName="ClientCard" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                                 </Trigger>
                                                 <DataTrigger Value="False">
                                                     <DataTrigger.Binding>
                                                         <MultiBinding Converter="{StaticResource PersonBillingComplianceConverter}">
                                                             <Binding />
                                                             <Binding Path="DataContext.BillingComplianceRequirements"
                                                                      RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                                         </MultiBinding>
                                                     </DataTrigger.Binding>
                                                     <Setter TargetName="ClientCard" Property="Background" Value="{DynamicResource OverdueFillBrush}" />
                                                     <Setter TargetName="ClientCard" Property="BorderBrush" Value="{DynamicResource OverdueBorderBrush}" />
                                                 </DataTrigger>
                                             </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding FullName}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   TextTrimming="CharacterEllipsis" />
                                        <TextBlock FontSize="11"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   Margin="0,3,0,0">
                                                            <Run Text="{Binding Waiver, Mode=OneWay}" />
                                            <Run Text=" · " />
                                                            <Run Text="{Binding EffectiveDate, Mode=OneWay, StringFormat={}{0:MM/dd/yyyy}}" />
                                        </TextBlock>
                                        <TextBlock Text="Billing attention required"
                                                   FontSize="10"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource DangerStrongBrush}"
                                                   Margin="0,3,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Value="False">
                                                            <DataTrigger.Binding>
                                                                <MultiBinding Converter="{StaticResource PersonBillingComplianceConverter}">
                                                                    <Binding />
                                                                    <Binding Path="DataContext.BillingComplianceRequirements"
                                                                             RelativeSource="{RelativeSource AncestorType=ListBox}" />
                                                                </MultiBinding>
                                                            </DataTrigger.Binding>
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1"
                                               Text="&#xE76C;"
                                               FontFamily="Segoe MDL2 Assets"
                                               FontSize="11"
                                               Foreground="{DynamicResource TextMutedBrush}"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- ═══ CLIENT DETAIL PANEL ═══ -->
            <!-- Always-visible compact selector. It remains available even when
                 no consumer workspace is currently shown. -->
            <Border Grid.Column="2"
                    Grid.Row="0"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="10,8"
                    Margin="0,0,0,10"
                    Visibility="{Binding UseHorizontalClientSelector, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="250" />
                        <ColumnDefinition Width="18" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="4" Orientation="Horizontal">
                        <Button Style="{StaticResource IconButton}"
                                AutomationProperties.Name="Add a new client"
                                Command="{Binding OpenEntryPanelCommand}"
                                ToolTip="Add a new client"
                                Content="Add client"
                                ContentTemplate="{StaticResource AddClientIconTemplate}" />
                        <Button Style="{StaticResource IconButton}"
                                AutomationProperties.Name="Expand client list"
                                Command="{Binding ToggleClientListCommand}"
                                IsEnabled="{Binding CanToggleClientList}"
                                ToolTip="Show full client list">
                            <TextBlock Text="&#xE76C;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                       Foreground="{DynamicResource TextSecondaryBrush}" />
                        </Button>
                    </StackPanel>
                    <TextBlock Grid.Column="0" Text="CLIENT" FontSize="11" FontWeight="Bold"
                               Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"
                               Margin="0,0,10,0" />
                    <ComboBox Grid.Column="1"
                              ItemsSource="{Binding People}"
                              SelectedItem="{Binding SelectedPerson, Mode=TwoWay}"
                              DisplayMemberPath="FullName"
                              IsEnabled="{Binding IsClientEditorOpen, Converter={StaticResource InverseBoolConverter}}"
                              AutomationProperties.Name="Select client" />
                    <ItemsControl Grid.Column="3"
                                  ItemsSource="{Binding SelectedPersonUpcomingItems}"
                                  VerticalAlignment="Center">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="1" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{DynamicResource DividerBrush}"
                                        BorderThickness="1,0,0,0"
                                        Padding="12,0"
                                        Margin="0">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding Date, StringFormat={}{0:MMM d}}"
                                                   FontSize="10"
                                                   Foreground="{DynamicResource TextSecondaryBrush}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Grid.Column="3"
                               Text="No upcoming items in the current work window"
                               Foreground="{DynamicResource TextMutedBrush}"
                               FontSize="11"
                               VerticalAlignment="Center"
                               Visibility="{Binding HasSelectedPersonUpcomingItems, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                </Grid>
            </Border>

            <Border Grid.Column="2"
                    Grid.Row="1"
                    Background="{DynamicResource SurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6">
                <Border.Style>
                    <Style TargetType="Border"
                           BasedOn="{StaticResource CompactClientPanelPadding}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ShowClientWorkspace}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="12" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0" />
                        <ColumnDefinition Width="0" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- CLIENT INFO -->
                    <ScrollViewer Grid.Column="0"
                                  Grid.Row="0"
                                  Grid.RowSpan="3"
                                  Visibility="Collapsed"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,0,0,12">
                            <TextBlock Text="{Binding SelectedPerson.FullName}"
                                       FontSize="15"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource TextPrimaryBrush}" />
                        </StackPanel>

                        <TextBlock Text="WAIVER"
                                   Style="{StaticResource LabelStyle}" />
                        <TextBlock Text="{Binding SelectedPerson.Waiver}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   Margin="0,0,0,10" />

                        <TextBlock Text="EFFECTIVE DATE"
                                   Style="{StaticResource LabelStyle}" />
                        <TextBlock Text="{Binding SelectedPerson.EffectiveDate, StringFormat={}{0:MM/dd/yyyy}}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   Margin="0,0,0,10" />

                        <TextBlock Text="DATE OF BIRTH"
                                   Style="{StaticResource LabelStyle}" />
                        <TextBlock Text="{Binding SelectedPerson.BirthDate, StringFormat={}{0:MM/dd/yyyy}}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   Margin="0,0,0,10" />

                        <TextBlock Text="LAST CONTACT"
                                   Style="{StaticResource LabelStyle}" />
                        <TextBlock Text="{Binding LastContact, StringFormat={}{0:MM/dd/yyyy}, TargetNullValue=—}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   Margin="0,0,0,10" />

                        <TextBlock Text="EVERGREEN ID"
                                   Style="{StaticResource LabelStyle}" />
                        <TextBlock Text="{Binding SelectedPerson.EvergreenId}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   Margin="0,0,0,10" />

                        <TextBlock Text="PHONE"
                                   Style="{StaticResource LabelStyle}" />
                        <TextBlock Text="{Binding SelectedPerson.PhoneNumber}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   Margin="0,0,0,10" />
                        
                        <TextBlock Text="ADDRESS"
                                   Style="{StaticResource LabelStyle}" />
                        <TextBlock Text="{Binding SelectedPerson.Address}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,10" />

                        <TextBlock Text="PRIMARY CARE PROVIDER"
                                   Style="{StaticResource LabelStyle}" />
                        <TextBlock Text="{Binding SelectedPerson.PrimaryCareProvider}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   Margin="0,0,0,10" />

                        <TextBlock Text="HEALTHCARE SYSTEM"
                                   Style="{StaticResource LabelStyle}" />
                        <TextBlock Text="{Binding SelectedPerson.HealthcareSystemName}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   Margin="0,0,0,10" />

                        <TextBlock Text="GUARDIAN"
                                   Style="{StaticResource LabelStyle}"
                                   Visibility="{Binding SelectedPerson.HasGuardian, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <TextBlock Text="{Binding SelectedPerson.GuardianName}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   Margin="0,0,0,10"
                                   Visibility="{Binding SelectedPerson.HasGuardian, Converter={StaticResource BoolToVisibilityConverter}}" />

                        <TextBlock Text="Open with VR"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,10"
                                   Visibility="{Binding SelectedPerson.OpenWithVR, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <StackPanel Visibility="{Binding SelectedPerson.OpenWithVR, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="VOCATIONAL REHABILITATION COUNSELOR (VRC)"
                                       Style="{StaticResource LabelStyle}" />
                            <TextBlock Text="{Binding SelectedPerson.VrCounselorName, TargetNullValue=—}"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontSize="13"
                                       Margin="0,0,0,10" />
                            <TextBlock Text="{Binding VrAssistantTitle}"
                                       Style="{StaticResource LabelStyle}" />
                            <TextBlock Text="{Binding SelectedPerson.VrAssistantName, TargetNullValue=—}"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       FontSize="13"
                                       Margin="0,0,0,10" />
                        </StackPanel>

                        <TextBlock Text="SERVICES"
                                   Style="{StaticResource LabelStyle}"
                                   Visibility="{Binding HasSelectedPersonServices, Converter={StaticResource BoolToVisibilityConverter}}" />
                        <TextBlock Text="{Binding SelectedPersonServices}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,10"
                                   Visibility="{Binding HasSelectedPersonServices, Converter={StaticResource BoolToVisibilityConverter}}" />

                        <TextBlock Text="Employment tracking required"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,10"
                                   Visibility="{Binding ShowsEmploymentTracking, Converter={StaticResource BoolToVisibilityConverter}}" />

                        <TextBlock Text="BIO"
                                   Style="{StaticResource LabelStyle}" />
                        <TextBlock Text="{Binding SelectedPerson.Bio}"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   FontSize="13"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                    </ScrollViewer>

                    <TabControl Grid.Column="2"
                                Grid.Row="0"
                                Grid.RowSpan="3"
                                SelectedIndex="{Binding ClientWorkspaceTabIndex}"
                                Style="{StaticResource ClientWorkspaceTabControlStyle}"
                                AutomationProperties.Name="Consumer record sections">
                        <TabItem Header="Overview"
                                  Style="{StaticResource ClientWorkspaceTabItemStyle}">
                            <ScrollViewer VerticalScrollBarVisibility="Visible"
                                          HorizontalScrollBarVisibility="Disabled"
                                          AutomationProperties.Name="Consumer overview">
                            <DockPanel LastChildFill="True">
                                <TextBlock DockPanel.Dock="Top" Text="{Binding AnnualDocuments.Reminder}"
                                           TextWrapping="Wrap" Margin="0,0,0,10"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           AutomationProperties.Name="Annual document preparation reminder" />
                                <Border DockPanel.Dock="Top"
                                        Background="{DynamicResource DangerSoftBrush}"
                                        BorderBrush="{DynamicResource OverdueBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="12,9"
                                        Margin="0,0,0,12"
                                        Visibility="{Binding HasSelectedPersonComplianceIssues,
                                            Converter={StaticResource BoolToVisibilityConverter}}"
                                        AutomationProperties.Name="Selected client billing compliance alert">
                                    <StackPanel>
                                        <TextBlock Text="BILLING COMPLIANCE ATTENTION"
                                                   FontSize="11"
                                                   FontWeight="Bold"
                                                   Foreground="{DynamicResource DangerStrongBrush}" />
                                        <TextBlock Text="Resolve the following before this client can move through ordinary billing approval:"
                                                   Margin="0,3,0,3"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource DangerStrongBrush}"
                                                   TextWrapping="Wrap" />
                                        <ItemsControl ItemsSource="{Binding SelectedPersonComplianceReasons}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Margin="8,1,0,0"
                                                               FontSize="11"
                                                               Foreground="{DynamicResource DangerStrongBrush}"
                                                               TextWrapping="Wrap">
                                                        <Run Text="• " />
                                                        <Run Text="{Binding Mode=OneWay}" />
                                                    </TextBlock>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                                <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="12" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="12" />
                                    <RowDefinition Height="Auto" MinHeight="220" />
                                    <RowDefinition Height="12" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                    <!-- Landscape consumer summary. This replaces the former vertical
                         identity rail so Overview retains substantially more height. -->
                    <Border Grid.Row="0"
                            Background="{DynamicResource PersonGroupAltBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="7"
                            Padding="14"
                            Visibility="{Binding IsClientEditorOpen, Converter={StaticResource InverseBoolToVisibilityConverter}}"
                            AutomationProperties.Name="Selected consumer details">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.ColumnSpan="4"
                                       Text="{Binding SelectedPerson.FullName}"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       Margin="0,0,0,10"
                                       AutomationProperties.HeadingLevel="Level2" />
                            <Button Grid.Row="0" Grid.Column="3"
                                    Style="{StaticResource IconButton}"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Top"
                                    Command="{Binding BeginClientEditCommand}"
                                    AutomationProperties.Name="Unlock client details for editing"
                                    ToolTip="Edit client details">
                                <TextBlock Text="&#xE72E;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Foreground="{DynamicResource TextSecondaryBrush}" />
                            </Button>

                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,14,10">
                                <TextBlock Text="WAIVER" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding SelectedPerson.Waiver}" Foreground="{DynamicResource TextPrimaryBrush}" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,14,10">
                                <TextBlock Text="EFFECTIVE DATE" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding SelectedPerson.EffectiveDate, StringFormat={}{0:MM/dd/yyyy}, TargetNullValue=—}"
                                           Foreground="{DynamicResource TextPrimaryBrush}" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,14,10">
                                <TextBlock Text="DATE OF BIRTH" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding SelectedPerson.BirthDate, StringFormat={}{0:MM/dd/yyyy}}"
                                           Foreground="{DynamicResource TextPrimaryBrush}" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="3" Margin="0,0,0,10">
                                <TextBlock Text="LAST CONTACT" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding LastContact, StringFormat={}{0:MM/dd/yyyy}, TargetNullValue=—}"
                                           Foreground="{DynamicResource TextPrimaryBrush}" />
                            </StackPanel>

                            <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,14,10">
                                <TextBlock Text="EVERGREEN ID" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding SelectedPerson.EvergreenId, TargetNullValue=—}"
                                           Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,14,10">
                                <TextBlock Text="PHONE" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding SelectedPerson.PhoneNumber, TargetNullValue=—}"
                                           Foreground="{DynamicResource TextPrimaryBrush}" />
                            </StackPanel>
                            <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,0,14,10">
                                <TextBlock Text="PRIMARY CARE PROVIDER" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding SelectedPerson.PrimaryCareProvider, TargetNullValue=—}"
                                           Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <StackPanel Grid.Row="2" Grid.Column="3" Margin="0,0,0,10">
                                <TextBlock Text="HEALTHCARE SYSTEM" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding SelectedPerson.HealthcareSystemName, TargetNullValue=—}"
                                           Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" />
                            </StackPanel>

                            <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,18,0">
                                <TextBlock Text="ADDRESS" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding SelectedPerson.Address, TargetNullValue=—}"
                                           Foreground="{DynamicResource TextPrimaryBrush}" TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,0,14,0">
                                <TextBlock Text="GUARDIAN / STATUS" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding SelectedPerson.GuardianName}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           TextWrapping="Wrap"
                                           Visibility="{Binding SelectedPerson.HasGuardian, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <TextBlock Text="No guardian recorded"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Visibility="{Binding SelectedPerson.HasGuardian, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                                <TextBlock Text="Open with VR"
                                           Foreground="{DynamicResource AccentPressedBrush}"
                                           FontWeight="SemiBold"
                                           Visibility="{Binding SelectedPerson.OpenWithVR, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <TextBlock Text="{Binding SelectedPerson.VrCounselorName, StringFormat=VRC: {0}}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           TextWrapping="Wrap"
                                           Visibility="{Binding SelectedPerson.OpenWithVR, Converter={StaticResource BoolToVisibilityConverter}}" />
                                <TextBlock Foreground="{DynamicResource TextPrimaryBrush}"
                                           TextWrapping="Wrap"
                                           Visibility="{Binding SelectedPerson.OpenWithVR, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Run Text="{Binding VrAssistantTitle, Mode=OneWay}" />
                                    <Run Text=": " />
                                    <Run Text="{Binding SelectedPerson.VrAssistantName, Mode=OneWay}" />
                                </TextBlock>
                                <TextBlock Text="Employment tracking required"
                                           Foreground="{DynamicResource WarningBrush}"
                                           FontWeight="SemiBold"
                                           TextWrapping="Wrap"
                                           Visibility="{Binding ShowsEmploymentTracking, Converter={StaticResource BoolToVisibilityConverter}}" />
                            </StackPanel>
                            <StackPanel Grid.Row="3" Grid.Column="3">
                                <TextBlock Text="SERVICES" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding SelectedPersonServices, TargetNullValue=No services recorded}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Row="4" Grid.ColumnSpan="4" Margin="0,10,0,0">
                                <TextBlock Text="BIO" Style="{StaticResource LabelStyle}" />
                                <TextBlock Text="{Binding SelectedPerson.Bio, TargetNullValue=No biography recorded}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           TextWrapping="Wrap"
                                           MaxHeight="42" />
                            </StackPanel>

                            <Border Grid.Row="5" Grid.ColumnSpan="4"
                                    Margin="0,14,0,0"
                                    Padding="12"
                                    Background="{DynamicResource SurfaceBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="7"
                                    AutomationProperties.Name="Representative payee profile summary">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.1*" />
                                        <ColumnDefinition Width="0.8*" />
                                        <ColumnDefinition Width="2.1*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                        <TextBlock Text="CASE MANAGER IS REPRESENTATIVE PAYEE"
                                                   Style="{StaticResource LabelStyle}" />
                                        <TextBlock Text="Yes"
                                                   FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   Visibility="{Binding SelectedPerson.CaseManagerIsRepPayee, Converter={StaticResource BoolToVisibilityConverter}}" />
                                        <TextBlock Text="No"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   Visibility="{Binding SelectedPerson.CaseManagerIsRepPayee, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="0,0,16,0"
                                                Visibility="{Binding SelectedPerson.CaseManagerIsRepPayee, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="MONTHLY INCOME" Style="{StaticResource LabelStyle}" />
                                        <TextBlock Text="{Binding SelectedPerson.RepPayeeMonthlyIncome, StringFormat={}{0:C2}, TargetNullValue=—}"
                                                   Foreground="{DynamicResource TextPrimaryBrush}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2"
                                                Visibility="{Binding SelectedPerson.CaseManagerIsRepPayee, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="REGULAR CHECK-REQUEST NEEDS" Style="{StaticResource LabelStyle}" />
                                        <TextBlock Text="{Binding SelectedPerson.RepPayeeRegularCheckRequestNeeds, TargetNullValue=—}"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   TextWrapping="Wrap"
                                                   MaxHeight="52" />
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Social Security number.
                                 Deliberately NOT part of the demographic editor above.
                                 It saves through its own audited route, and folding it
                                 into the ordinary profile save would put the number in a
                                 DTO that travels everywhere and lose the audit event that
                                 records who set it.

                                 The mask is what is displayed. The number appears only
                                 while the case manager explicitly shows it, for reading to
                                 the Social Security Administration, and hiding clears it
                                 from the view model rather than merely from view. -->
                            <Border Grid.Row="6" Grid.ColumnSpan="4"
                                    Margin="0,14,0,0"
                                    Padding="16"
                                    CornerRadius="12"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    Background="{DynamicResource SurfaceBrush}"
                                    Visibility="{Binding SsnPanel.SupportsStorage, Converter={StaticResource BoolToVisibilityConverter}}">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="Social Security number"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextPrimaryBrush}" />
                                            <TextBlock Text="Encrypted identity information"
                                                       Margin="0,2,0,0"
                                                       FontSize="11"
                                                       Foreground="{DynamicResource TextSecondaryBrush}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Border Padding="12,6"
                                                    CornerRadius="12"
                                                    Background="{DynamicResource PersonGroupAltBrush}">
                                                <TextBlock Text="{Binding SsnPanel.Masked}"
                                                           FontSize="14"
                                                           FontWeight="SemiBold"
                                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                                           AutomationProperties.Name="Social Security number on file" />
                                            </Border>
                                            <TextBlock Text="{Binding SsnPanel.Revealed}"
                                                       FontSize="14"
                                                       FontWeight="Bold"
                                                       Margin="12,0,0,0"
                                                       VerticalAlignment="Center"
                                                       Foreground="{DynamicResource AccentPressedBrush}"
                                                       AutomationProperties.Name="Full Social Security number, currently shown"
                                                       Visibility="{Binding SsnPanel.IsRevealed, Converter={StaticResource BoolToVisibilityConverter}}" />
                                            <Button Content="Show"
                                                    Style="{StaticResource SsnActionButtonStyle}"
                                                    Margin="10,0,0,0"
                                                    Command="{Binding SsnPanel.RevealCommand}"
                                                    AutomationProperties.Name="Show the full Social Security number"
                                                    AutomationProperties.HelpText="Recorded as a disclosure in the audit trail."
                                                    Visibility="{Binding SsnPanel.IsRevealed, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                                            <Button Content="Hide"
                                                    Style="{StaticResource SsnActionButtonStyle}"
                                                    Margin="10,0,0,0"
                                                    Command="{Binding SsnPanel.HideCommand}"
                                                    AutomationProperties.Name="Hide the full Social Security number"
                                                    Visibility="{Binding SsnPanel.IsRevealed, Converter={StaticResource BoolToVisibilityConverter}}" />
                                        </StackPanel>
                                    </Grid>

                                    <Grid Margin="0,14,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="220" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox Style="{StaticResource SsnFieldStyle}"
                                                 Text="{Binding SsnPanel.Entry, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="Enter a new Social Security number"
                                                 AutomationProperties.HelpText="Nine digits. Dashes optional." />
                                        <Button Grid.Column="1"
                                                Content="Save securely"
                                                Style="{StaticResource SsnPrimaryButtonStyle}"
                                                Margin="10,0,0,0"
                                                Command="{Binding SsnPanel.SaveCommand}"
                                                AutomationProperties.Name="Save the Social Security number" />
                                        <Button Grid.Column="2"
                                                Content="Remove"
                                                Style="{StaticResource SsnActionButtonStyle}"
                                                Margin="10,0,0,0"
                                                Command="{Binding SsnPanel.ClearCommand}"
                                                AutomationProperties.Name="Remove the stored Social Security number" />
                                    </Grid>

                                    <TextBlock Text="{Binding SsnPanel.StatusMessage}"
                                               Margin="0,6,0,0"
                                               TextWrapping="Wrap"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Visibility="{Binding SsnPanel.HasStatusMessage, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    <TextBlock Text="{Binding SsnPanel.Explanation}"
                                               Margin="0,4,0,0"
                                               TextWrapping="Wrap"
                                               FontSize="11"
                                               Foreground="{DynamicResource TextSecondaryBrush}" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- The same information becomes an inline editor when unlocked.
                         No secondary edit panel or detached copy of the client exists. -->
                    <Border Grid.Row="0"
                            Background="{DynamicResource PersonGroupAltBrush}"
                            BorderBrush="{DynamicResource AccentBrush}"
                            BorderThickness="1"
                            CornerRadius="7"
                             Padding="14"
                             Visibility="{Binding IsClientEditorOpen, Converter={StaticResource BoolToVisibilityConverter}}"
                             AutomationProperties.Name="Client details edit mode">
                        <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Row="0" Grid.ColumnSpan="2" VerticalAlignment="Center">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xE785;"
                                                   FontFamily="Segoe MDL2 Assets"
                                                   FontSize="14"
                                                   Foreground="{DynamicResource AccentPressedBrush}"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"
                                                   AutomationProperties.Name="Client details unlocked" />
                                        <TextBlock Text="{Binding EntryPanelHeader}"
                                                   FontSize="16"
                                                   FontWeight="Bold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   VerticalAlignment="Center"
                                                   AutomationProperties.HeadingLevel="Level2" />
                                    </StackPanel>
                                    <Border Margin="0,9,12,0"
                                            Padding="10,7"
                                            HorizontalAlignment="Left"
                                            Background="{DynamicResource SurfaceBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="5"
                                            AutomationProperties.Name="Required field completion guide"
                                            AutomationProperties.HelpText="Orange items still need information. Green items are complete. Representative-payee income and needs are required only when Yes is selected.">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="*"
                                                           Foreground="{DynamicResource WarningBrush}"
                                                           FontWeight="Bold"
                                                           Margin="0,0,5,0" />
                                                <TextBlock Text="REQUIRED TO SAVE"
                                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                                           FontSize="11"
                                                           FontWeight="SemiBold" />
                                            </StackPanel>
                                            <WrapPanel Margin="0,5,0,0">
                                                <TextBlock Text="● First name" Margin="0,0,14,0" FontWeight="SemiBold">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{DynamicResource WarningBrush}" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsFirstNameReady}" Value="True">
                                                                    <Setter Property="Foreground" Value="{DynamicResource SuccessStrongBrush}" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <TextBlock Text="● Last name" Margin="0,0,14,0" FontWeight="SemiBold">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{DynamicResource WarningBrush}" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsLastNameReady}" Value="True">
                                                                    <Setter Property="Foreground" Value="{DynamicResource SuccessStrongBrush}" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <TextBlock Text="● Date of birth" Margin="0,0,14,0" FontWeight="SemiBold">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{DynamicResource WarningBrush}" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsBirthDateReady}" Value="True">
                                                                    <Setter Property="Foreground" Value="{DynamicResource SuccessStrongBrush}" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <TextBlock Text="● Biography" FontWeight="SemiBold">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{DynamicResource WarningBrush}" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsBioReady}" Value="True">
                                                                    <Setter Property="Foreground" Value="{DynamicResource SuccessStrongBrush}" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </WrapPanel>
                                            <TextBlock Text="All other details are optional. Representative-payee income and needs become required only when Yes is selected."
                                                       Margin="0,5,0,0"
                                                       FontSize="10.5"
                                                       Foreground="{DynamicResource TextMutedBrush}"
                                                       TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>
                                    <Border Margin="0,8,12,0"
                                            Padding="10,7"
                                            HorizontalAlignment="Left"
                                            Background="{DynamicResource WarningFillBrush}"
                                            BorderBrush="{DynamicResource WarningBrush}"
                                            BorderThickness="1"
                                            CornerRadius="5"
                                            Visibility="{Binding CanMarkNewConsumerAsTest, Converter={StaticResource BoolToVisibilityConverter}}"
                                            AutomationProperties.Name="Test consumer creation option">
                                        <StackPanel>
                                            <CheckBox Content="Test consumer"
                                                      IsChecked="{Binding IsTestData}"
                                                      FontWeight="SemiBold"
                                                      AutomationProperties.Name="Mark new consumer as Test" />
                                            <TextBlock Text="Use only for synthetic records created for testing. This designation cannot be added or removed after creation."
                                                       Margin="22,3,0,0"
                                                       FontSize="10.5"
                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                       TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>
                                    <Border Background="{DynamicResource WarningFillBrush}"
                                            BorderBrush="{DynamicResource WarningBrush}"
                                            BorderThickness="1"
                                            CornerRadius="5"
                                            Padding="9"
                                            Margin="0,8,12,0"
                                            Visibility="{Binding HasClientSaveError, Converter={StaticResource BoolToVisibilityConverter}}"
                                            AutomationProperties.LiveSetting="Assertive"
                                            AutomationProperties.Name="Client save problem">
                                        <TextBlock Text="{Binding ClientSaveErrorMessage}"
                                                   Foreground="{DynamicResource WarningBrush}"
                                                   TextWrapping="Wrap" />
                                    </Border>
                                </StackPanel>
                                <StackPanel Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2"
                                            Orientation="Horizontal"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top">
                                    <!-- Import fills the current form; Submit remains the only
                                         writer. Existing-profile use is agency-disabled by default
                                         and mismatched Credible ids are refused by the view model. -->
                                    <Button Content="{Binding CredibleImportActionLabel}"
                                            Command="{Binding ConsumerImport.ChooseFileCommand}"
                                            AutomationProperties.Name="Fill this form from a saved Credible export"
                                            ToolTip="Fill this form from a saved Credible print view"
                                            Style="{StaticResource FormActionButton}"
                                            Visibility="{Binding CanImportCredibleIntoCurrentForm, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    <Button Content="Cancel"
                                            Command="{Binding CancelClientEditCommand}"
                                            Style="{StaticResource FormActionButton}" />
                                    <Button Content="{Binding SubmitButtonLabel}"
                                            Command="{Binding SubmitCommand}"
                                            Style="{StaticResource FormActionPrimaryButton}"
                                            AutomationProperties.Name="{Binding SubmitButtonLabel}" />
                                </StackPanel>

                                <!-- The review panel and any refusal, spanning the form's width
                                     directly under the action row so they cannot be scrolled past. -->
                                <ContentControl Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4"
                                                Margin="0,36,12,0"
                                                Content="{Binding ConsumerImport}"
                                                Visibility="{Binding ConsumerImport.IsOpen, Converter={StaticResource BoolToVisibilityConverter}}" />

                                <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4"
                                        Background="{DynamicResource WarningFillBrush}"
                                        BorderBrush="{DynamicResource WarningBrush}"
                                        BorderThickness="1"
                                        CornerRadius="5"
                                        Padding="9"
                                        Margin="0,36,12,0"
                                        AutomationProperties.LiveSetting="Assertive"
                                        AutomationProperties.Name="Credible import problem"
                                        Visibility="{Binding ConsumerImport.HasRefusal, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock Text="{Binding ConsumerImport.RefusalMessage}"
                                               Foreground="{DynamicResource WarningBrush}"
                                               TextWrapping="Wrap" />
                                </Border>

                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,12,14,0">
                                    <TextBlock Style="{StaticResource LabelStyle}">
                                        <Run Text="FIRST NAME" /><Run Text=" *" Foreground="{DynamicResource WarningBrush}" />
                                    </TextBlock>
                                    <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="First name, required" />
                                </StackPanel>
                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,12,14,0">
                                    <TextBlock Style="{StaticResource LabelStyle}">
                                        <Run Text="LAST NAME" /><Run Text=" *" Foreground="{DynamicResource WarningBrush}" />
                                    </TextBlock>
                                    <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Last name, required" />
                                </StackPanel>
                                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,12,14,0">
                                    <TextBlock Style="{StaticResource LabelStyle}">
                                        <Run Text="DATE OF BIRTH" /><Run Text=" *" Foreground="{DynamicResource WarningBrush}" />
                                    </TextBlock>
                                    <DatePicker SelectedDate="{Binding BirthDate}" AutomationProperties.Name="Date of birth, required" />
                                </StackPanel>
                                <StackPanel Grid.Row="1" Grid.Column="3" Margin="0,12,0,0">
                                    <TextBlock Text="WAIVER" Style="{StaticResource LabelStyle}" />
                                    <ComboBox ItemsSource="{Binding Waivers}" SelectedItem="{Binding Waiver}" AutomationProperties.Name="Waiver" />
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,10,14,0" IsEnabled="{Binding HasWaiver}">
                                    <TextBlock Text="EFFECTIVE DATE (MM/DD)" Style="{StaticResource LabelStyle}" />
                                    <TextBox Text="{Binding EffectiveDateText, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Effective date" />
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,10,14,0">
                                    <TextBlock Text="EVERGREEN ID" Style="{StaticResource LabelStyle}" />
                                    <TextBox Text="{Binding EvergreenId, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Evergreen ID" />
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,10,14,0">
                                    <TextBlock Text="GENDER / PRONOUNS" Style="{StaticResource LabelStyle}" />
                                    <ComboBox ItemsSource="{Binding Genders}" SelectedItem="{Binding Gender}"
                                              AutomationProperties.Name="Gender and pronouns" />
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="3" Margin="0,10,0,0">
                                    <TextBlock Text="HEALTHCARE SYSTEM" Style="{StaticResource LabelStyle}" />
                                    <ComboBox ItemsSource="{Binding HealthcareSystems}"
                                              DisplayMemberPath="Name" SelectedValuePath="Name"
                                              SelectedValue="{Binding HealthcareSystemName}"
                                              AutomationProperties.Name="Healthcare system" />
                                </StackPanel>

                                <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,10,14,0">
                                    <TextBlock Text="ADDRESS" Style="{StaticResource LabelStyle}" />
                                    <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Address" />
                                    <TextBlock Style="{StaticResource LabelStyle}" Margin="0,10,0,5">
                                        <Run Text="BIO" /><Run Text=" *" Foreground="{DynamicResource WarningBrush}" />
                                    </TextBlock>
                                    <TextBox Text="{Binding Bio, UpdateSourceTrigger=PropertyChanged}"
                                             AcceptsReturn="True" TextWrapping="Wrap" MinHeight="62"
                                             SpellCheck.IsEnabled="True" AutomationProperties.Name="Biography, required" />
                                </StackPanel>
                                <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,10,14,0">
                                    <TextBlock Text="PHONE" Style="{StaticResource LabelStyle}" />
                                    <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"
                                             AutomationProperties.Name="Phone number" />
                                    <TextBlock Text="EMAIL (OPTIONAL)" Style="{StaticResource LabelStyle}" />
                                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                             MaxLength="254"
                                             AutomationProperties.Name="Client email, optional"
                                             AutomationProperties.HelpText="Leave blank when no email address is available." />
                                    <TextBlock Text="PRIMARY CARE PROVIDER" Style="{StaticResource LabelStyle}" />
                                    <TextBox Text="{Binding PrimaryCareProvider, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Primary care provider" />
                                    <CheckBox Content="Has guardian" IsChecked="{Binding HasGuardian}" Margin="0,10,0,4" />
                                    <TextBox Text="{Binding GuardianName, UpdateSourceTrigger=PropertyChanged}"
                                             IsEnabled="{Binding HasGuardian}" AutomationProperties.Name="Guardian name" />
                                    <CheckBox Content="Open with VR" IsChecked="{Binding OpenWithVR}" Margin="0,10,0,4" />
                                    <StackPanel Visibility="{Binding OpenWithVR, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="VOCATIONAL REHABILITATION COUNSELOR (VRC)"
                                                   Style="{StaticResource LabelStyle}" />
                                        <TextBox Text="{Binding VrCounselorName, UpdateSourceTrigger=PropertyChanged}"
                                                 MaxLength="150"
                                                 AutomationProperties.Name="Vocational Rehabilitation Counselor" />
                                        <TextBlock Text="{Binding VrAssistantTitle}"
                                                   Style="{StaticResource LabelStyle}" />
                                        <TextBox Text="{Binding VrAssistantName, UpdateSourceTrigger=PropertyChanged}"
                                                 MaxLength="150"
                                                 AutomationProperties.Name="Vocational Rehabilitation assistant assignment" />
                                    </StackPanel>
                                    <CheckBox Content="Employed" IsChecked="{Binding IsEmployed}" />
                                    <CheckBox Content="Employment specialist" IsChecked="{Binding HasEmploymentSpecialist}" IsEnabled="{Binding IsEmployed}" />
                                    <CheckBox Content="Work supports" IsChecked="{Binding HasWorkSupports}" IsEnabled="{Binding IsEmployed}" />
                                </StackPanel>
                                <StackPanel Grid.Row="3" Grid.Column="3" Margin="0,10,0,0">
                                    <TextBlock Text="WAIVER SERVICES" Style="{StaticResource LabelStyle}" />
                                    <CheckBox Content="Home support" IsChecked="{Binding HasHomeSupport}" />
                                    <CheckBox Content="Self-directed home support" IsChecked="{Binding HasSelfDirectedHomeSupport}" />
                                    <CheckBox Content="Shared living" IsChecked="{Binding HasSharedLiving}" />
                                    <CheckBox Content="Community support 1:1" IsChecked="{Binding HasCommunitySupport1To1}" />
                                    <CheckBox Content="Community support self-directed" IsChecked="{Binding HasCommunitySupportSelfDirected}" />
                                    <CheckBox Content="Day program" IsChecked="{Binding HasCommunitySupportDayProgram}" />
                                    <StackPanel Orientation="Horizontal" Margin="20,0,0,0"
                                                Visibility="{Binding HasCommunitySupportDayProgram, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="Programs" VerticalAlignment="Center" Margin="0,0,6,0" />
                                        <TextBox Text="{Binding DayProgramCount, UpdateSourceTrigger=LostFocus}" Width="42" AutomationProperties.Name="Day program count" />
                                    </StackPanel>
                                </StackPanel>

                                <Grid Grid.Row="4" Grid.ColumnSpan="4" Margin="0,14,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1.2*" />
                                        <ColumnDefinition Width="0.8*" />
                                        <ColumnDefinition Width="0.7*" />
                                        <ColumnDefinition Width="1.5*" />
                                        <ColumnDefinition Width="1*" />
                                        <ColumnDefinition Width="0.45*" />
                                        <ColumnDefinition Width="0.75*" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                        <TextBlock Text="MAINECARE ID" Style="{StaticResource LabelStyle}" />
                                        <TextBox Text="{Binding MaineCareId, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="MaineCare member ID" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                        <TextBlock Text="DIAGNOSIS" Style="{StaticResource LabelStyle}" />
                                        <TextBox Text="{Binding DiagnosisCode, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="Billing diagnosis code" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" Margin="0,0,10,0">
                                        <TextBlock Text="PLACE" Style="{StaticResource LabelStyle}" />
                                        <TextBox Text="{Binding PlaceOfService, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="Two digit place of service" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="3" Margin="0,0,10,0">
                                        <TextBlock Text="CLAIM STREET" Style="{StaticResource LabelStyle}" />
                                        <TextBox Text="{Binding BillingStreet, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="Subscriber claim street" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="4" Margin="0,0,10,0">
                                        <TextBlock Text="CITY" Style="{StaticResource LabelStyle}" />
                                        <TextBox Text="{Binding BillingCity, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="Subscriber claim city" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="5" Margin="0,0,10,0">
                                        <TextBlock Text="STATE" Style="{StaticResource LabelStyle}" />
                                        <TextBox Text="{Binding BillingState, UpdateSourceTrigger=PropertyChanged}" MaxLength="2"
                                                 AutomationProperties.Name="Subscriber claim state" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="6">
                                        <TextBlock Text="ZIP" Style="{StaticResource LabelStyle}" />
                                        <TextBox Text="{Binding BillingZip, UpdateSourceTrigger=PropertyChanged}"
                                                 AutomationProperties.Name="Subscriber claim ZIP code" />
                                    </StackPanel>
                                </Grid>

                                <Border Grid.Row="5" Grid.ColumnSpan="4"
                                        Margin="0,16,0,0"
                                        Padding="14"
                                        Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="7"
                                        AutomationProperties.Name="Representative payee profile editor">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1.05*" />
                                            <ColumnDefinition Width="0.85*" />
                                            <ColumnDefinition Width="2.1*" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,18,0">
                                            <TextBlock Text="REPRESENTATIVE PAYEE" Style="{StaticResource LabelStyle}" />
                                            <TextBlock Text="Is the case manager the consumer's representative payee?"
                                                       TextWrapping="Wrap"
                                                       Foreground="{DynamicResource TextPrimaryBrush}" />
                                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                                <RadioButton Content="Yes"
                                                             GroupName="RepresentativePayeeChoice"
                                                             IsChecked="{Binding CaseManagerIsRepPayee, Mode=TwoWay}"
                                                             Margin="0,0,18,0"
                                                             AutomationProperties.Name="Case manager is representative payee, yes" />
                                                <RadioButton Content="No"
                                                             GroupName="RepresentativePayeeChoice"
                                                             IsChecked="{Binding CaseManagerIsRepPayee, Mode=TwoWay, Converter={StaticResource InverseBoolConverter}}"
                                                             AutomationProperties.Name="Case manager is representative payee, no" />
                                            </StackPanel>
                                        </StackPanel>

                                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,18,0"
                                                    Visibility="{Binding CaseManagerIsRepPayee, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <TextBlock Style="{StaticResource LabelStyle}">
                                                <Run Text="MONTHLY INCOME" /><Run Text=" *" Foreground="{DynamicResource WarningBrush}" />
                                            </TextBlock>
                                            <TextBox Text="{Binding RepPayeeMonthlyIncomeText, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                                     MaxLength="30"
                                                     AutomationProperties.Name="Consumer monthly income managed by representative payee"
                                                     AutomationProperties.HelpText="Enter dollars and cents, for example 943.50." />
                                        </StackPanel>

                                        <StackPanel Grid.Row="0" Grid.Column="2"
                                                    Visibility="{Binding CaseManagerIsRepPayee, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <TextBlock Style="{StaticResource LabelStyle}">
                                                <Run Text="REGULAR CHECK-REQUEST NEEDS" /><Run Text=" *" Foreground="{DynamicResource WarningBrush}" />
                                            </TextBlock>
                                            <TextBox Text="{Binding RepPayeeRegularCheckRequestNeeds, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                                     AcceptsReturn="True"
                                                     TextWrapping="Wrap"
                                                     MinHeight="58"
                                                     MaxLength="2000"
                                                     SpellCheck.IsEnabled="True"
                                                     AutomationProperties.Name="Regular representative-payee check-request needs"
                                                     AutomationProperties.HelpText="Describe recurring needs, or enter None when there are no recurring requests." />
                                        </StackPanel>

                                        <TextBlock Grid.Row="1" Grid.ColumnSpan="3"
                                                   Margin="0,10,0,0"
                                                   Text="This profile records recurring needs only. It does not request or authorize a check."
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   FontSize="11"
                                                   TextWrapping="Wrap"
                                                   Visibility="{Binding CaseManagerIsRepPayee, Converter={StaticResource BoolToVisibilityConverter}}" />
                                    </Grid>
                                </Border>
                        </Grid>
                    </Border>

                    <!-- The two expandable profile panels intentionally come last.
                         Forms and the working Notes/Journal area stay ahead of reference
                         details that can be reached by scrolling when needed. -->
                    <StackPanel Grid.Row="6">

                    <!-- Live support-network profile. Notes snapshot selected
                         attendees, so edits here never rewrite historical visits. -->
                    <Border Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="7"
                            Padding="12"
                            AutomationProperties.Name="Consumer contacts and support team">
                        <Expander IsExpanded="True">
                            <Expander.Header>
                                <DockPanel LastChildFill="True">
                                    <Button DockPanel.Dock="Right"
                                            Content="Add Contact"
                                            Command="{Binding BeginAddContactCommand}"
                                            Style="{StaticResource PrimaryButton}"
                                            Padding="14,5"
                                            Margin="12,0,0,0"
                                            AutomationProperties.Name="Add a consumer contact" />
                                    <StackPanel>
                                        <TextBlock Text="CONTACTS &amp; SUPPORT TEAM"
                                                   Style="{StaticResource SectionHeader}"
                                                   Margin="0" />
                                        <TextBlock Text="People recorded here can be selected as attendees when documenting a visit."
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   FontSize="11"
                                                   TextWrapping="Wrap" />
                                    </StackPanel>
                                </DockPanel>
                            </Expander.Header>

                            <StackPanel Margin="0,10,0,0">
                                <ScrollViewer MaxHeight="150"
                                              VerticalScrollBarVisibility="Auto">
                                    <ItemsControl ItemsSource="{Binding Contacts}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="0,0,0,6"
                                                        Padding="9,7"
                                                        Background="{DynamicResource PersonGroupAltBrush}"
                                                        BorderBrush="{DynamicResource DividerBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="5">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="2*" />
                                                            <ColumnDefinition Width="2*" />
                                                            <ColumnDefinition Width="2*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                                            <TextBlock Text="{Binding FullName}"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{DynamicResource TextPrimaryBrush}" />
                                                            <TextBlock Text="{Binding Kind, Converter={StaticResource EnumDescriptionConverter}}"
                                                                       FontSize="11"
                                                                       Foreground="{DynamicResource TextSecondaryBrush}" />
                                                        </StackPanel>
                                                        <StackPanel Grid.Column="1" Margin="0,0,12,0">
                                                            <TextBlock Text="{Binding Relationship, TargetNullValue=Relationship not specified}"
                                                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                                                       TextTrimming="CharacterEllipsis" />
                                                            <TextBlock Text="{Binding Organization}"
                                                                       FontSize="11"
                                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                                       TextTrimming="CharacterEllipsis" />
                                                        </StackPanel>
                                                        <StackPanel Grid.Column="2" Margin="0,0,12,0">
                                                            <TextBlock Text="{Binding Phone}"
                                                                       Foreground="{DynamicResource TextPrimaryBrush}" />
                                                            <TextBlock Text="{Binding Email}"
                                                                       FontSize="11"
                                                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                                                       TextTrimming="CharacterEllipsis" />
                                                        </StackPanel>
                                                        <Button Grid.Column="3"
                                                                Content="Edit"
                                                                Padding="12,4"
                                                                VerticalAlignment="Center"
                                                                Command="{Binding DataContext.BeginEditContactCommand,
                                                                    RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                                CommandParameter="{Binding}"
                                                                AutomationProperties.Name="Edit this contact" />
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>

                                <Border Margin="0,4,0,0"
                                        Padding="10"
                                        Background="{DynamicResource SurfaceAltBrush}"
                                        BorderBrush="{DynamicResource AccentBrush}"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Visibility="{Binding IsContactEditorOpen,
                                            Converter={StaticResource BoolToVisibilityConverter}}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="10" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="10" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="10" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Row="0" Grid.ColumnSpan="3"
                                                   Text="{Binding ContactEditorHeader}"
                                                   FontWeight="Bold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   VerticalAlignment="Center" />
                                        <StackPanel Grid.Row="0" Grid.Column="4" Grid.ColumnSpan="3"
                                                    Orientation="Horizontal"
                                                    HorizontalAlignment="Right">
                                            <Button Content="Cancel"
                                                    Command="{Binding CancelContactEditCommand}"
                                                    Padding="12,4"
                                                    Margin="0,0,8,0" />
                                            <Button Content="{Binding ContactSaveButtonLabel}"
                                                    Command="{Binding SaveContactCommand}"
                                                    Style="{StaticResource PrimaryButton}"
                                                    Padding="14,4" />
                                        </StackPanel>

                                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,8,0,0">
                                            <TextBlock Text="FIRST NAME" Style="{StaticResource LabelStyle}" />
                                            <TextBox Text="{Binding ContactFirstName, UpdateSourceTrigger=PropertyChanged}"
                                                     AutomationProperties.Name="Contact first name" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,8,0,0">
                                            <TextBlock Text="LAST NAME" Style="{StaticResource LabelStyle}" />
                                            <TextBox Text="{Binding ContactLastName, UpdateSourceTrigger=PropertyChanged}"
                                                     AutomationProperties.Name="Contact last name" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="1" Grid.Column="4" Margin="0,8,0,0">
                                            <TextBlock Text="CONTACT TYPE" Style="{StaticResource LabelStyle}" />
                                            <ComboBox ItemsSource="{Binding ContactKinds}"
                                                      SelectedItem="{Binding ContactKind}"
                                                      AutomationProperties.Name="Contact type">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                        <StackPanel Grid.Row="1" Grid.Column="6" Margin="0,8,0,0">
                                            <TextBlock Text="RELATIONSHIP / ROLE" Style="{StaticResource LabelStyle}" />
                                            <TextBox Text="{Binding ContactRelationship, UpdateSourceTrigger=PropertyChanged}"
                                                     AutomationProperties.Name="Contact relationship or role" />
                                        </StackPanel>

                                        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,8,0,0">
                                            <TextBlock Text="ORGANIZATION" Style="{StaticResource LabelStyle}" />
                                            <TextBox Text="{Binding ContactOrganization, UpdateSourceTrigger=PropertyChanged}"
                                                     AutomationProperties.Name="Contact organization" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,8,0,0">
                                            <TextBlock Text="PHONE" Style="{StaticResource LabelStyle}" />
                                            <TextBox Text="{Binding ContactPhone, UpdateSourceTrigger=PropertyChanged}"
                                                     AutomationProperties.Name="Contact phone" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="2" Grid.Column="4" Margin="0,8,0,0">
                                            <TextBlock Text="EMAIL" Style="{StaticResource LabelStyle}" />
                                            <TextBox Text="{Binding ContactEmail, UpdateSourceTrigger=PropertyChanged}"
                                                     AutomationProperties.Name="Contact email" />
                                        </StackPanel>
                                        <StackPanel Grid.Row="2" Grid.Column="6" Margin="0,8,0,0">
                                            <CheckBox Content="Emergency contact"
                                                      IsChecked="{Binding ContactIsEmergencyContact}" />
                                            <CheckBox Content="Active information release"
                                                      IsChecked="{Binding ContactHasActiveRelease}" />
                                        </StackPanel>
                                        <TextBlock Grid.Row="3" Grid.ColumnSpan="7"
                                                   Margin="0,7,0,0"
                                                   Text="{Binding ContactStatusMessage}"
                                                   Foreground="{DynamicResource DangerStrongBrush}"
                                                   AutomationProperties.LiveSetting="Polite" />
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Expander>
                    </Border>

                    <!-- Medical providers. Its own control so the panel can be loaded and
                         asserted on without the rest of the profile; see
                         ConsumerProvidersView.xaml for why that matters. -->
                    <views:ConsumerProvidersView DataContext="{Binding ConsumerProviders}" />

                    </StackPanel>

                    <!-- FORMS COMPLIANCE -->
                    <Grid Grid.Row="2">
                        <Grid.Resources>
                            <!-- The shared checkbox style leaves generous breathing room
                                 elsewhere. Forms is a dense status matrix, so one pixel is
                                 enough separation and avoids manufacturing another scrollbar. -->
                            <Style TargetType="CheckBox"
                                   BasedOn="{StaticResource {x:Type CheckBox}}">
                                <Setter Property="Margin" Value="0,0,0,1" />
                            </Style>
                        </Grid.Resources>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0"
                                    Orientation="Horizontal"
                                    Margin="0,0,0,8">
                            <TextBlock Text="FORMS"
                                       Style="{StaticResource SectionHeader}"
                                       Margin="0,0,12,0"
                                       VerticalAlignment="Center" />
                        <Button Style="{StaticResource IconButton}"
                                AutomationProperties.Name="Toggle compliance editing"
                                    Command="{Binding ToggleComplianceOverrideCommand}"
                                    ToolTip="Toggle compliance editing">
                                <TextBlock FontFamily="Segoe MDL2 Assets"
                                           FontSize="14"
                                           Foreground="{DynamicResource TextSecondaryBrush}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text"
                                                    Value="&#xE72E;" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding AllowComplianceOverride}"
                                                             Value="True">
                                                    <Setter Property="Text"
                                                            Value="&#xE785;" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Button>
                        </StackPanel>

                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="8" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0"
                                           Grid.Column="0"
                                           Text="90-Day Reviews"
                                           Style="{StaticResource SectionHeader}"
                                           Margin="0,0,0,2" />
                                <StackPanel Grid.Row="1"
                                            Grid.Column="0">
                                    <CheckBox Content="Quarter 1"
                                              IsChecked="{Binding Q1RCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.Q1R}" />
                                    <TextBlock Text="{Binding Q1RDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>
                                <StackPanel Grid.Row="2"
                                            Grid.Column="0">
                                    <CheckBox Content="Quarter 2"
                                              IsChecked="{Binding Q2RCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.Q2R}" />
                                    <TextBlock Text="{Binding Q2RDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>
                                <StackPanel Grid.Row="3"
                                            Grid.Column="0">
                                    <CheckBox Content="Quarter 3"
                                              IsChecked="{Binding Q3RCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.Q3R}" />
                                    <TextBlock Text="{Binding Q3RDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>
                                <StackPanel Grid.Row="4"
                                            Grid.Column="0">
                                    <CheckBox Content="Quarter 4"
                                              IsChecked="{Binding Q4RCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.Q4R}" />
                                    <TextBlock Text="{Binding Q4RDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>

                                <TextBlock Grid.Row="0"
                                           Grid.Column="2"
                                           Text="Annual Forms"
                                           Style="{StaticResource SectionHeader}"
                                           Margin="0,0,0,2" />
                                <StackPanel Grid.Row="1"
                                            Grid.Column="2">
                                    <CheckBox Content="PCP"
                                              IsChecked="{Binding PcpCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.PCP}" />
                                    <TextBlock Text="{Binding PcpDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>
                                <StackPanel Grid.Row="2"
                                            Grid.Column="2">
                                    <CheckBox Content="Comp Assessment"
                                              IsChecked="{Binding CompAssessmentCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.ComprehensiveAssessment}" />
                                    <TextBlock Text="{Binding CompAssessmentDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>
                                <StackPanel Grid.Row="3"
                                            Grid.Column="2">
                                    <CheckBox Content="Reclassification"
                                              IsChecked="{Binding ReclassificationCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.Reclassification}" />
                                    <TextBlock Text="{Binding ReclassificationDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>
                                <StackPanel Grid.Row="4"
                                            Grid.Column="2">
                                    <CheckBox Content="Safety Plan"
                                              IsChecked="{Binding SafetyPlanCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.SafetyPlan}" />
                                    <TextBlock Text="{Binding SafetyPlanDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>
                                <StackPanel Grid.Row="5"
                                            Grid.Column="2">
                                    <CheckBox Content="Privacy Practices"
                                              IsChecked="{Binding PrivacyPracticesCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.PrivacyPractices}" />
                                    <TextBlock Text="{Binding PrivacyPracticesDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>

                                <TextBlock Grid.Row="0"
                                           Grid.Column="4"
                                           Text="Releases"
                                           Style="{StaticResource SectionHeader}"
                                           Margin="0,0,0,2" />
                                <StackPanel Grid.Row="1"
                                            Grid.Column="4">
                                    <CheckBox Content="DHHS"
                                              IsChecked="{Binding ReleaseDhhsCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.Release_DHHS}" />
                                    <TextBlock Text="{Binding ReleaseDhhsDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>
                                <StackPanel Grid.Row="2"
                                            Grid.Column="4">
                                    <CheckBox Content="Agency"
                                              IsChecked="{Binding ReleaseAgencyCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.Release_Agency}" />
                                    <TextBlock Text="{Binding ReleaseAgencyDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>
                                <StackPanel Grid.Row="3"
                                            Grid.Column="4">
                                    <CheckBox Content="Medical"
                                              IsChecked="{Binding ReleaseMedicalCompliant, Mode=OneWay}"
                                              IsEnabled="{Binding AllowComplianceOverride}"
                                              Command="{Binding ToggleFormCommand}"
                                              CommandParameter="{x:Static local:FormType.Release_Medical}" />
                                    <TextBlock Text="{Binding ReleaseMedicalDueDate, StringFormat={}{0:MM/dd/yy}}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               Margin="20,0,0,1" />
                                </StackPanel>

                                <!-- Appointments: latest doctor/dentist for the
                                     selected client. Date uses OverdueBrush when >365 days
                                     stale (per category); dash when none on record.
                                     Read on selection, not from the caseload load. -->
                                <TextBlock Grid.Row="0"
                                           Grid.Column="6"
                                           Text="Appointments"
                                           Style="{StaticResource SectionHeader}"
                                           Margin="0,0,0,2" />
                                <StackPanel Grid.Row="1"
                                            Grid.Column="6">
                                    <TextBlock Text="Doctor"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               FontSize="13" />
                                    <TextBlock Text="{Binding LastDoctorDate, StringFormat={}{0:MM/dd/yy}, TargetNullValue=—}"
                                               FontSize="11"
                                               Margin="20,1,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDoctorOverdue}" Value="True">
                                                        <Setter Property="Foreground" Value="{DynamicResource OverdueBrush}" />
                                                        <Setter Property="FontWeight" Value="SemiBold" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding DoctorName}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               TextWrapping="Wrap"
                                               Margin="20,0,0,4" />
                                </StackPanel>
                                <StackPanel Grid.Row="2"
                                            Grid.Column="6">
                                    <TextBlock Text="Dentist"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               FontSize="13" />
                                    <TextBlock Text="{Binding LastDentistDate, StringFormat={}{0:MM/dd/yy}, TargetNullValue=—}"
                                               FontSize="11"
                                               Margin="20,1,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDentistOverdue}" Value="True">
                                                        <Setter Property="Foreground" Value="{DynamicResource OverdueBrush}" />
                                                        <Setter Property="FontWeight" Value="SemiBold" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding DentistName}"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               FontSize="11"
                                               TextWrapping="Wrap"
                                               Margin="20,0,0,4" />
                                </StackPanel>
                            </Grid>
                        <views:FormAttestationControl Grid.Row="2"
                                                      DataContext="{Binding Attestation}" />
                    </Grid>

                    <!-- NOTES HISTORY + JOURNAL — split the bottom-right cell so the
                         per-consumer journal sits beside the read-only notes list. The
                         3*/2* split gives notes the larger share; both scale with the
                         panel. Journal auto-saves 2s after typing stops (debounced in
                         the VM), plus on client-switch and shutdown — no Save button,
                         matching the app's auto-save-over-dialogs convention. -->
                    <Grid Grid.Row="4"
                          MinHeight="220">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>

                        <!-- NOTES HISTORY -->
                        <Grid Grid.Column="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Text="NOTES"
                                       Style="{StaticResource SectionHeader}" />

                            <DataGrid AutomationProperties.Name="Selected person notes" Grid.Row="1"
                                      MinHeight="180"
                                      ItemsSource="{Binding SelectedPersonNotes}"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      RowBackground="{DynamicResource SurfaceBrush}"
                                      AlternatingRowBackground="{DynamicResource WindowBackgroundBrush}"
                                      GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="{DynamicResource DividerBrush}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Date"
                                                        Binding="{Binding EventDate, StringFormat={}{0:MM/dd}}"
                                                        Width="60" />
                                    <DataGridTextColumn Header="Status"
                                                        Binding="{Binding Status}"
                                                        Width="80" />
                                    <DataGridTextColumn Header="Narrative"
                                        Binding="{Binding Narrative}"
                                        Visibility="{Binding Data.ShowNarrativeColumn, Source={StaticResource LayoutProxy}, Converter={StaticResource BoolToVisibilityConverter}}"
                                        Width="*" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>

                        <!-- JOURNAL -->
                        <Grid Grid.Column="2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0">
                                <TextBlock Text="JOURNAL"
                                           Style="{StaticResource SectionHeader}" />
                                <TextBlock Text="{Binding JournalSaveWarning}"
                                           Margin="0,-4,0,6"
                                           FontSize="10"
                                           Foreground="{DynamicResource DangerStrongBrush}"
                                           TextWrapping="Wrap"
                                           Visibility="{Binding HasJournalSaveWarning,
                                               Converter={StaticResource BoolToVisibilityConverter}}"
                                           AutomationProperties.Name="Journal cloud save warning" />
                            </StackPanel>

                            <!-- Pattern layer sits UNDER the TextBox in the same cell:
                                 the dot brush fills a Border, the TextBox draws over it
                                 with the theme's translucent SurfaceAlpha70Brush so the
                                 dots read faintly through the fill without dimming text.
                                 The Border carries the visible frame now, so the TextBox
                                 drops its own border to avoid a double edge. -->
                            <Border Grid.Row="1"
                                    Background="{StaticResource JournalDotBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="2" />

                            <!-- UpdateSourceTrigger=PropertyChanged so the debounce
                                 timer sees each keystroke; without it the VM's Journal
                                 wouldn't update until focus loss and the 2s save
                                 wouldn't fire mid-typing. Indefinitely long: wraps and
                                 scrolls vertically, no height cap of its own — the row's
                                 * share bounds it and the ScrollViewer takes overflow. -->
                            <TextBox AutomationProperties.Name="Journal" Grid.Row="1"
                                     MinHeight="180"
                                     Text="{Binding Journal, UpdateSourceTrigger=PropertyChanged}"
                                     IsEnabled="{Binding CanEditJournal}"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     SpellCheck.IsEnabled="True"
                                     FontFamily="{StaticResource NarrativeFont}"
                                     Background="{DynamicResource SurfaceAlpha25Brush}"
                                     BorderThickness="0"
                                     Padding="8" />
                        </Grid>
                    </Grid>
                            </Grid>
                            </DockPanel>
                            </ScrollViewer>
                        </TabItem>

                        <TabItem Header="Comprehensive Assessment"
                                 Style="{StaticResource ClientWorkspaceTabItemStyle}">
                            <clientDocuments:ComprehensiveAssessmentWorkspace />
                        </TabItem>

                        <TabItem Header="Person-Centered Plan"
                                 Style="{StaticResource ClientWorkspaceTabItemStyle}">
                            <clientDocuments:PersonCenteredPlanWorkspace />
                        </TabItem>

                        <TabItem Header="Classification"
                                 Style="{StaticResource ClientWorkspaceTabItemStyle}">
                            <clientDocuments:ClassificationWorkspace />
                        </TabItem>

                        <TabItem Header="DHHS Forms"
                                 Style="{StaticResource ClientWorkspaceTabItemStyle}">
                            <clientDocuments:DhhsFormsWorkspace DataContext="{Binding DhhsForms}" />
                        </TabItem>

                        <TabItem Header="Agency Release"
                                 Style="{StaticResource ClientWorkspaceTabItemStyle}">
                            <clientDocuments:AgencyReleaseWorkspace DataContext="{Binding AgencyRelease}" />
                        </TabItem>

                        <TabItem Header="Safety Plan" Style="{StaticResource ClientWorkspaceTabItemStyle}">
                            <clientDocuments:SafetyPlanWorkspace DataContext="{Binding SafetyPlan}" />
                        </TabItem>
                        <TabItem Header="Annual Documents" Style="{StaticResource ClientWorkspaceTabItemStyle}">
                            <clientDocuments:AnnualDocumentsWorkspace DataContext="{Binding AnnualDocuments}" />
                        </TabItem>

                        <TabItem Header="Check Requests" Style="{StaticResource ClientWorkspaceTabItemStyle}">
                            <clientDocuments:CheckRequestsWorkspace DataContext="{Binding CheckRequests}" />
                        </TabItem>

                        <!-- AT REQUESTS
                             Every assistive technology payment request filed for
                             this client, newest first. The list follows the CLIENT
                             rather than whoever currently carries them, so a
                             transfer does not orphan their filed documents.

                             "Regenerate PDF" rebuilds the document from the stored
                             record, not from today's figures — see
                             ATRequestPdfExporter. -->
                        <TabItem Header="AT Requests"
                                 Style="{StaticResource ClientWorkspaceTabItemStyle}">
                            <Grid Margin="16">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0"
                                           Text="Assistive technology payment requests filed for this client. Regenerating a published request reproduces the document exactly as it was submitted."
                                           TextWrapping="Wrap"
                                           FontSize="12"
                                           LineHeight="17"
                                           Margin="0,0,0,12"
                                           Foreground="{DynamicResource TextSecondaryBrush}" />

                                <TextBlock Grid.Row="1"
                                           Text="No AT requests have been created for this client."
                                           VerticalAlignment="Top"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Visibility="{Binding HasSelectedPersonAtRequests,
                                               Converter={StaticResource InverseBoolToVisibilityConverter}}" />

                                <DataGrid Grid.Row="1"
                                          ItemsSource="{Binding SelectedPersonAtRequests}"
                                          AutoGenerateColumns="False"
                                          IsReadOnly="True"
                                          HeadersVisibility="Column"
                                          CanUserAddRows="False"
                                          GridLinesVisibility="Horizontal"
                                          AutomationProperties.Name="AT requests for this client"
                                          Visibility="{Binding HasSelectedPersonAtRequests,
                                              Converter={StaticResource BoolToVisibilityConverter}}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Submitted"
                                                            Width="110"
                                                            Binding="{Binding SubmittedDate, StringFormat=yyyy-MM-dd}" />
                                        <DataGridTextColumn Header="Status"
                                                            Width="100"
                                                            Binding="{Binding Status}" />
                                        <DataGridTextColumn Header="Vendor"
                                                            Width="*"
                                                            Binding="{Binding VendorName}" />
                                        <DataGridTextColumn Header="Total"
                                                            Width="100"
                                                            Binding="{Binding TotalCost, StringFormat=C}" />
                                        <!-- Who attested, not who the client's case
                                             manager happens to be now. Blank on a draft,
                                             which is itself the useful signal. -->
                                        <DataGridTextColumn Header="Published by"
                                                            Width="150"
                                                            Binding="{Binding SignedByName}" />
                                        <DataGridTemplateColumn Header="Document"
                                                                Width="140">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="Regenerate PDF"
                                                            Style="{StaticResource SmallButton}"
                                                            Margin="4,2"
                                                            AutomationProperties.Name="{Binding SubmittedDate,
                                                                StringFormat='Regenerate PDF for the request submitted {0:yyyy-MM-dd}'}"
                                                            Command="{Binding DataContext.RegenerateAtRequestPdfCommand,
                                                                RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                            CommandParameter="{Binding}" />
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </TabItem>
                    </TabControl>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
